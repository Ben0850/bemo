---
phase: 06-detail-ui-zahlungserfassung
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/js/app.js
autonomous: true
requirements: [PAY-STAT-04, PAY-UI-05]

must_haves:
  truths:
    - "Beim Öffnen einer Rechnungs-Detailseite sieht der Anwender direkt unter dem Page-Header einen prominenten Status-Badge (offen/teilbezahlt/bezahlt/ueberzahlt)"
    - "Direkt neben dem Status-Badge stehen 'Bereits bezahlt: X €' und 'Offener Betrag: Y €' mit den korrekten Zahlen aus dem Server"
    - "'Offener Betrag' wird berechnet als total_gross - payment_saldo und kann negativ sein bei Überzahlung"
    - "Status-Badge im Detail-Header und Status-Badge in der Rechnungs-Liste (Phase 5) zeigen für dieselbe Rechnung denselben Wert"
    - "loadInvoicePayments(invoiceId) gibt das Array der Zahlungen zurück (verwendet von Plan 06-02)"
    - "loadBankAccounts() gibt das Array der Bankkonten zurück und cached den Wert für die Modal-Verwendung in Plan 06-02"
  artifacts:
    - path: "public/js/app.js"
      provides: "Saldo-Header-Block in renderInvoiceDetail() + Daten-Loader loadInvoicePayments + loadBankAccounts"
      contains:
        - "function loadInvoicePayments"
        - "function loadBankAccounts"
        - "function renderInvoicePaymentSaldoHeader"
        - "Bereits bezahlt"
        - "Offener Betrag"
  key_links:
    - from: "renderInvoiceDetail() in app.js"
      to: "GET /api/invoices/:id Response (payment_saldo + payment_status)"
      via: "inv.payment_saldo und inv.payment_status werden gerendert"
      pattern: "inv\\.payment_(saldo|status)"
    - from: "renderInvoicePaymentSaldoHeader()"
      to: "getPaymentStatusBadge() Helper (existiert seit Phase 5 Plan 02)"
      via: "Direkter Funktionsaufruf"
      pattern: "getPaymentStatusBadge\\(.*payment_status.*\\)"
---

<objective>
Header-Block mit prominenter Saldo- und Status-Anzeige in der Rechnungs-Detailseite einfügen, plus die zwei Daten-Loader-Funktionen, die Plan 06-02 für die Zahlungstabelle und das Konto-Dropdown wiederverwenden wird.

Purpose: PAY-STAT-04 verlangt, dass der Anwender den Zahlungsstatus *beim Öffnen* einer Rechnung sofort prominent sieht — nicht erst nach dem Scrollen zur Zahlungstabelle. PAY-UI-05 verlangt zusätzlich Bereits-bezahlt + Offener-Betrag im selben Header-Bereich. Daten-Loader werden hier vorbereitet, damit Plan 06-02 (Tabelle + Form + CRUD) auf etablierten Funktionen aufsetzen kann statt Loader und UI parallel zu bauen.

Output: 3 neue Funktionen in public/js/app.js (loadInvoicePayments, loadBankAccounts, renderInvoicePaymentSaldoHeader) plus eine Anpassung an renderInvoiceDetail(), die den Header-Block direkt unter dem Page-Header (vor dem Kundendaten-Card) einfügt.
</objective>

<execution_context>
@C:/Users/BenAlbers/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/BenAlbers/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

@.planning/phases/05-status-logik-listen/05-01-SUMMARY.md
@.planning/phases/05-status-logik-listen/05-02-SUMMARY.md

@public/js/app.js
@server.js

<interfaces>
<!-- Aus Phase 4 Plan 02 (server.js): GET /api/invoices/:id/payments -->
<!-- Response: 200 + Array, sortiert nach payment_date ASC -->
<!-- Bei unbekannter Rechnung: 404 + {error:'Rechnung nicht gefunden'} -->
Payment-Datensatz aus GET /api/invoices/:id/payments:
{
  id: number,
  invoice_id: number,
  direction: 'in' | 'out',
  amount: number,                 // immer > 0
  payment_date: string,           // 'YYYY-MM-DD'
  payment_method: string,         // 'Überweisung' | 'Bar' | 'Kartenzahlung' | 'Lastschrift' | 'SEPA' | 'Sonstige' | ''
  bank_account_id: number | null, // null = Bar/Kasse
  bank_account_label: string|null,// LEFT JOIN aus bank_accounts (Phase 4 Plan 02)
  bank_account_iban:  string|null,// LEFT JOIN aus bank_accounts
  reference: string,
  notes: string,
  booked_by: string,              // x-user-name beim Anlegen
  created_at: string,
  updated_at: string
}

<!-- Aus Phase 5 Plan 01 (server.js): GET /api/invoices/:id Response -->
{
  ...alle bisherigen Invoice-Felder (id, invoice_number, customer_*, total_net, total_gross, total_vat, status, items[], ...),
  payment_saldo: number,          // (SUM in - SUM out), gerundet auf 2 Nachkommastellen
  payment_status: 'offen' | 'teilbezahlt' | 'bezahlt' | 'ueberzahlt'
}

<!-- Aus server.js Zeile 788: GET /api/bank-accounts -->
Bank-Account-Datensatz:
{
  id: number,
  label: string,        // z.B. "Sparkasse Geschaeftskonto"
  iban: string,         // PFLICHT
  bic: string,
  bank_name: string,
  is_default: 0 | 1
}

<!-- Bestehende Helper aus app.js — wiederverwenden, NICHT duplizieren -->
function getPaymentStatusBadge(payment_status)  // Phase 5 Plan 02, Zeile 6120
function escapeHtml(s)
function formatDate(isoDate)                      // 'YYYY-MM-DD' -> 'DD.MM.YYYY'
function showToast(msg, type)
function api(path, opts)                          // setzt automatisch x-user-name + x-user-permission Header
function canEditInvoice()                         // returns true für Verwaltung/Buchhaltung/Admin (app.js Zeile 32)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: loadInvoicePayments + loadBankAccounts Daten-Loader hinzufügen</name>
  <files>public/js/app.js</files>

  <read_first>
    - public/js/app.js Zeilen 6115-6135 (getPaymentStatusBadge Pattern + globaler _allInvoices Cache als Vorlage für loadBankAccounts-Cache)
    - public/js/app.js Zeile 6271-6286 (openNewInvoiceModal — zeigt bestehendes Bank-Accounts-Lade-Pattern via api('/api/bank-accounts'))
    - .planning/phases/04-schema-backend/04-02-SUMMARY.md (GET /api/invoices/:id/payments Response-Format und 404-Verhalten)
  </read_first>

  <action>
    Zwei neue async Funktionen in public/js/app.js einfügen, direkt NACH der bestehenden Funktion `getPaymentStatusBadge` (also nach Zeile 6129, vor `let _allInvoices = [];` auf Zeile 6132).

    **Funktion 1 — loadInvoicePayments(invoiceId):**

    ```javascript
    // Phase 6 (PAY-UI-01..05): Lädt alle Zahlungen einer Rechnung.
    // Wird vom Header-Saldo-Block (Plan 06-01) und der Zahlungstabelle (Plan 06-02) genutzt.
    // Wirft eine Exception wenn die Rechnung nicht existiert (404 vom Backend).
    async function loadInvoicePayments(invoiceId) {
      return await api(`/api/invoices/${invoiceId}/payments`);
    }
    ```

    **Funktion 2 — loadBankAccounts() mit Modul-Cache:**

    ```javascript
    // Phase 6: Bankkonten-Cache fuer Zahlungs-Modal in Plan 06-02.
    // Konten aendern sich selten -- einmal pro Session laden reicht.
    // Aufruf von _bankAccountsCache=null (z.B. nach CRUD auf bank_accounts) erzwingt Reload.
    let _bankAccountsCache = null;
    async function loadBankAccounts() {
      if (_bankAccountsCache !== null) return _bankAccountsCache;
      try {
        _bankAccountsCache = await api('/api/bank-accounts');
      } catch (e) {
        _bankAccountsCache = []; // defensive: bei Fehler leeres Array, Modal kann trotzdem mit "Bar/Kasse" arbeiten
      }
      return _bankAccountsCache;
    }
    ```

    Wichtig:
    - KEIN Duplikat von getPaymentStatusBadge — der existiert bereits seit Phase 5 Plan 02 (Zeile 6120) und wird in Task 2 wiederverwendet.
    - KEIN eigenes Setzen von x-user-name/x-user-permission Headers — die bestehende `api()`-Funktion setzt sie automatisch (siehe MEMORY.md / STATE.md / 04-02-SUMMARY.md).
    - Beide Funktionen sind global (keine const/closure) — Plan 06-02 muss sie aufrufen können.
  </action>

  <verify>
    <automated>node --check public/js/app.js</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "async function loadInvoicePayments" public/js/app.js` returns 1
    - `grep -c "async function loadBankAccounts" public/js/app.js` returns 1
    - `grep -c "let _bankAccountsCache" public/js/app.js` returns 1
    - `grep -c "function getPaymentStatusBadge" public/js/app.js` returns 1 (KEIN Duplikat eingeführt)
    - `node --check public/js/app.js` exit 0
    - Beide neuen Funktionen sind oberhalb von `let _allInvoices` (also vor Zeile ~6132 platziert)
  </acceptance_criteria>

  <done>
    Zwei neue Loader-Funktionen sind global in app.js verfügbar, getPaymentStatusBadge ist unverändert (keine Duplikate), Syntax-Check ist grün.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Saldo-Header-Block in renderInvoiceDetail einfügen</name>
  <files>public/js/app.js</files>

  <read_first>
    - public/js/app.js Zeilen 6396-6492 (komplette renderInvoiceDetail) — Insertion-Punkt verstehen
    - public/js/app.js Zeile 6406-6414 (page-header — direkt darunter wird der Saldo-Block eingefügt)
    - public/js/app.js Zeile 6105-6129 (getInvoiceStatusBadge + getPaymentStatusBadge als Style-Vorbild)
    - .planning/phases/05-01-SUMMARY.md (payment_saldo + payment_status garantiert vorhanden in GET /api/invoices/:id Response)
  </read_first>

  <action>
    Zwei Änderungen in public/js/app.js:

    **Änderung A — Neue Funktion renderInvoicePaymentSaldoHeader(inv) hinzufügen:**

    Direkt nach loadBankAccounts (aus Task 1) einfügen, vor `let _allInvoices = [];`:

    ```javascript
    // Phase 6 (PAY-STAT-04 + PAY-UI-05): Prominenter Saldo+Status-Header oben in der Rechnungs-Detailseite.
    // Konsumiert payment_saldo + payment_status aus GET /api/invoices/:id (Phase 5 Plan 01).
    // Wiederverwendet getPaymentStatusBadge aus Phase 5 Plan 02 -- KEIN Duplikat.
    function renderInvoicePaymentSaldoHeader(inv) {
      const paid  = Math.round((Number(inv.payment_saldo)  || 0) * 100) / 100;
      const gross = Math.round((Number(inv.total_gross)    || 0) * 100) / 100;
      const open  = Math.round((gross - paid) * 100) / 100;        // kann negativ sein bei ueberzahlt
      const openColor = open < 0
        ? 'var(--text-muted)'                                      // ueberzahlt -> dezent
        : (open === 0 ? 'var(--success, #16a34a)' : 'var(--danger, #dc2626)');
      return `
        <div class="card payment-saldo-header" style="padding:16px 20px;display:flex;flex-wrap:wrap;align-items:center;gap:24px;">
          <div style="display:flex;align-items:center;gap:10px;">
            <span style="font-size:14px;color:var(--text-muted);">Zahlungsstatus:</span>
            <span style="font-size:16px;font-weight:600;">${getPaymentStatusBadge(inv.payment_status)}</span>
          </div>
          <div style="display:flex;align-items:center;gap:10px;">
            <span style="font-size:14px;color:var(--text-muted);">Bereits bezahlt:</span>
            <strong style="font-size:16px;">${paid.toFixed(2)} &euro;</strong>
          </div>
          <div style="display:flex;align-items:center;gap:10px;">
            <span style="font-size:14px;color:var(--text-muted);">Offener Betrag:</span>
            <strong style="font-size:16px;color:${openColor};">${open.toFixed(2)} &euro;</strong>
            ${open < 0 ? '<span style="font-size:12px;color:var(--text-muted);">(ueberzahlt)</span>' : ''}
          </div>
        </div>
      `;
    }
    ```

    **Änderung B — renderInvoiceDetail() Header-Block-Insertion (Zeile ~6414):**

    In der bestehenden `main.innerHTML = \`...\`` Template-Literal-Zuweisung der Funktion `renderInvoiceDetail` (Zeilen 6406-6488):

    Direkt NACH dem schließenden `</div>` des `page-header`-Blocks (also nach `</div>` auf Zeile 6414, vor dem ersten `<div class="card">` auf Zeile 6416), folgende Zeile einfügen:

    ```html
          ${renderInvoicePaymentSaldoHeader(inv)}
    ```

    So sieht der relevante Block danach aus:

    ```javascript
        main.innerHTML = `
          <a class="back-link" onclick="navigate('invoices')">&#8592; Zurück zur Liste</a>
          <div class="page-header">
            <h2>Rechnung ${escapeHtml(inv.invoice_number)}</h2>
            <div style="display:flex;gap:8px;">
              <button class="btn btn-primary" onclick="window.open('/api/invoices/${id}/pdf','_blank')">PDF anzeigen</button>
              ${isAdmin() ? `<button class="btn btn-danger" onclick="deleteInvoice(${id})">Löschen</button>` : ''}
            </div>
          </div>

          ${renderInvoicePaymentSaldoHeader(inv)}     // <-- NEU

          <div class="card">
            <div class="card-header"><h3>Kundendaten</h3></div>
            ...
    ```

    Wichtig:
    - KEIN neues CSS — wiederverwendet bestehende `card`-Klasse + Inline-Styles (analog zur bestehenden invoice-summary in renderInvoiceSummary).
    - KEINE manuelle Status-Berechnung — `inv.payment_status` und `inv.payment_saldo` kommen direkt aus der GET-Response (Phase 5 Plan 01 garantiert beide Felder).
    - `inv.total_gross` ist bereits in inv vorhanden (bestehender renderInvoiceSummary nutzt es).
    - Math.round * 100 / 100 für Saldo-Anzeige verhindert Floating-Point-Display-Artefakte (gleicher Pattern wie Phase 5 Plan 01 Backend).
    - `&euro;` als HTML-Entity konsistent mit bestehendem renderInvoiceSummary (Zeile 6519-6521).
  </action>

  <verify>
    <automated>node --check public/js/app.js</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "function renderInvoicePaymentSaldoHeader" public/js/app.js` returns 1
    - `grep -c "Bereits bezahlt:" public/js/app.js` returns 1
    - `grep -c "Offener Betrag:" public/js/app.js` returns 1
    - `grep -c "renderInvoicePaymentSaldoHeader(inv)" public/js/app.js` returns ≥ 1 (Aufruf in renderInvoiceDetail)
    - `grep -c "function getPaymentStatusBadge" public/js/app.js` returns 1 (KEIN Duplikat)
    - In renderInvoiceDetail: der String `renderInvoicePaymentSaldoHeader(inv)` steht ZWISCHEN dem schließenden `</div>` des page-header und dem ersten `<div class="card">` mit Kundendaten (manuell prüfen via grep -n)
    - `node --check public/js/app.js` exit 0
    - Browser-Smoke (manuell, nicht-blockierend bis Plan 06-02 Checkpoint): Detail einer Rechnung mit total_gross > 0 öffnen → Header-Block mit Status-Badge "Offen" + "Bereits bezahlt: 0.00 €" + "Offener Betrag: <total_gross>.00 €" (in Rot) sichtbar
  </acceptance_criteria>

  <done>
    Beim Öffnen einer Rechnungs-Detailseite ist der Saldo+Status-Header zwischen Page-Header und Kundendaten-Card sichtbar, zeigt korrekte Werte aus Backend-Response, Syntax-Check ist grün.
  </done>
</task>

</tasks>

<verification>
**Statische Checks (automatisiert):**
1. `node --check public/js/app.js` exit 0
2. Alle 4 neuen Funktionen vorhanden: loadInvoicePayments, loadBankAccounts, renderInvoicePaymentSaldoHeader (+ existierende getPaymentStatusBadge unangetastet)
3. `_bankAccountsCache` als globaler `let` deklariert
4. `renderInvoicePaymentSaldoHeader(inv)` wird in renderInvoiceDetail aufgerufen
5. Bestehende `getPaymentStatusBadge` Funktion EXAKT 1× definiert (keine Duplikate)

**Smoke-Test (browser, nicht-blockierend):**
- Server starten: `taskkill //F //IM node.exe; node server.js` (gemäß MEMORY-Pattern)
- Bestehende Rechnung mit Saldo öffnen → Header-Block muss erscheinen
- Werte vergleichen mit Listen-Ansicht (Phase 5): payment_status-Badge muss übereinstimmen
- Volle visuelle/E2E-Verifikation passiert im Plan 06-02 Checkpoint
</verification>

<success_criteria>
- PAY-STAT-04: Status-Badge prominent im Header der Detailseite ✓
- PAY-UI-05: "Bereits bezahlt: X €" und "Offener Betrag: Y €" über (=oberhalb) der zukünftigen Zahlungs-Tabelle (Plan 06-02 wird Tabelle darunter einfügen) ✓
- Wiederverwendung getPaymentStatusBadge aus Phase 5 (kein Duplikat) ✓
- Daten-Loader bereit für Plan 06-02 (loadInvoicePayments + loadBankAccounts mit Cache) ✓
- Bestehende renderInvoiceDetail-Struktur (Kundendaten / Rechnungsdaten / Positionen) unangetastet ✓
- Plan ist autonomous=true (keine Checkpoints — kommen in Plan 06-02 am Ende der Phase) ✓
</success_criteria>

<output>
Nach Abschluss erstelle `.planning/phases/06-detail-ui-zahlungserfassung/06-01-SUMMARY.md` gemäß Template (siehe summary.md). Pflicht-Felder im Frontmatter:
- requirements-completed: [PAY-STAT-04, PAY-UI-05]
- provides: 3 neue Funktionen + Header-Block in renderInvoiceDetail
- affects: [06-02-zahlungstabelle-und-form]
</output>
