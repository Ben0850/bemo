---
phase: 06-detail-ui-zahlungserfassung
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - public/js/app.js
autonomous: false
requirements: [PAY-UI-01, PAY-UI-02, PAY-UI-03, PAY-UI-04]

must_haves:
  truths:
    - "Auf der Rechnungs-Detailseite erscheint zwischen Rechnungsdaten/Positionen und PDF-Footer ein 'Zahlungen'-Block mit chronologischer Tabelle"
    - "Tabellen-Spalten: Datum / Richtung (Eingang/Ausgang) / Betrag / Konto / Zahlungsart / Buchungs-User / Notiz / Aktionen"
    - "Bestehende Zahlungen aus Phase 4 (POST-Tests) erscheinen sofort in der Tabelle"
    - "Buttons '+ Zahlungseingang' und '+ Zahlungsausgang' sind sichtbar und klickbar (nur für Verwaltung/Buchhaltung/Admin)"
    - "Klick auf '+ Zahlungseingang' öffnet Modal mit Datum=heute, Richtung=Eingang vorausgewählt, Betrag-Feld leer, Bankkonto-Dropdown gefüllt, Default-Konto vorausgewählt"
    - "Klick auf '+ Zahlungsausgang' öffnet dasselbe Modal mit Richtung=Ausgang vorausgewählt"
    - "Speichern legt eine neue Zahlung an, schließt das Modal, und Tabelle + Saldo-Header werden aktualisiert OHNE Page-Reload"
    - "Über jeder Zeile ist 'Bearbeiten' verfügbar, öffnet das Modal vorausgefüllt mit den existierenden Werten und PUTtet beim Speichern"
    - "Über jeder Zeile ist 'Löschen' verfügbar, zeigt confirm() mit Datum + Betrag, und entfernt nach Bestätigung den Eintrag samt Saldo-Update"
    - "Benutzer-Rolle (kein Verwaltung/Buchhaltung/Admin) sieht weder die '+'-Buttons noch Bearbeiten/Löschen-Buttons in den Zeilen"
    - "Volle 5-Status-Übergangs-Sequenz funktioniert: Brutto 1000€ → ohne Zahlung offen → +400€ teilbezahlt → +600€ bezahlt → +100€ ueberzahlt → Ausgang 100€ bezahlt (visuell verifiziert)"
  artifacts:
    - path: "public/js/app.js"
      provides: "Zahlungstabelle + Modal-Form + Save/Edit/Delete-Handler in der Rechnungs-Detailseite"
      contains:
        - "function renderInvoicePaymentsBlock"
        - "function refreshInvoicePaymentsBlock"
        - "function openInvoicePaymentForm"
        - "function saveInvoicePayment"
        - "function deleteInvoicePayment"
        - "+ Zahlungseingang"
        - "+ Zahlungsausgang"
  key_links:
    - from: "renderInvoiceDetail()"
      to: "renderInvoicePaymentsBlock + Auto-Load der Payments"
      via: "Async-Block direkt nach renderInvoicePaymentSaldoHeader, ruft loadInvoicePayments(id)"
      pattern: "loadInvoicePayments\\("
    - from: "saveInvoicePayment / deleteInvoicePayment"
      to: "Saldo-Header und Tabelle"
      via: "refreshInvoicePaymentsBlock(invoiceId) — re-fetcht GET /api/invoices/:id und GET /api/invoices/:id/payments und re-rendert beide Blöcke"
      pattern: "refreshInvoicePaymentsBlock\\("
    - from: "openInvoicePaymentForm"
      to: "Bank-Accounts-Dropdown"
      via: "loadBankAccounts() aus Plan 06-01"
      pattern: "loadBankAccounts\\("
    - from: "+ Zahlungseingang/Ausgang Buttons + Aktionen-Spalte"
      to: "Permission-Check"
      via: "canEditInvoice() — gleiche Rollen wie POST/PUT/DELETE Backend (Verwaltung/Buchhaltung/Admin)"
      pattern: "canEditInvoice\\(\\)"
---

<objective>
Den interaktiven Zahlungs-Block in der Rechnungs-Detailseite vollständig umsetzen: chronologische Tabelle aller Zahlungen, Buttons zum Anlegen Eingang/Ausgang, Modal-Formular mit allen Pflichtfeldern (Datum, Richtung, Betrag, Bankkonto, Zahlungsart, Notiz), Inline-Bearbeiten/Löschen pro Zeile mit Bestätigungs-Dialog, permission-aware UI und partielles Re-Rendering ohne Page-Reload. Am Ende: human-verify Checkpoint mit voller 5-Status-Übergangs-E2E-Sequenz (laut 05-02-SUMMARY.md "Konsequenz fuer Phase 6" zwingend nachzuholen).

Purpose: Das ist das primäre Anwender-Deliverable des Milestones v1.1. Bis hierhin ist Zahlungserfassung nur via curl/Postman möglich. Mit diesem Plan kann der Anwender im Browser klicken und sofort sehen, wie sich Saldo+Status verändern.

Output: 5 neue Funktionen in public/js/app.js (renderInvoicePaymentsBlock, refreshInvoicePaymentsBlock, openInvoicePaymentForm, saveInvoicePayment, deleteInvoicePayment) plus eine Erweiterung von renderInvoiceDetail() (Tabellen-Block einfügen + initialer Auto-Load der Payments).
</objective>

<execution_context>
@C:/Users/BenAlbers/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/BenAlbers/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

@.planning/phases/04-schema-backend/04-02-SUMMARY.md
@.planning/phases/05-status-logik-listen/05-01-SUMMARY.md
@.planning/phases/05-status-logik-listen/05-02-SUMMARY.md
@.planning/phases/06-detail-ui-zahlungserfassung/06-01-PLAN.md

@public/js/app.js
@server.js

<interfaces>
<!-- Aus Plan 06-01 verfügbar (vorausgesetzt durch depends_on=[06-01]) -->
async function loadInvoicePayments(invoiceId)
  -> Promise<Payment[]> (siehe Payment-Struktur unten)
  -> Wirft Exception bei 404

async function loadBankAccounts()
  -> Promise<BankAccount[]>
  -> Liefert gecachten Wert (gleicher Modul-Cache _bankAccountsCache)

function renderInvoicePaymentSaldoHeader(inv)
  -> Returns string (HTML)
  -> Bereits in renderInvoiceDetail integriert (NICHT erneut einfügen)

<!-- Plan 06-02 muss dafür sorgen, dass refreshInvoicePaymentsBlock auch den Saldo-Header neu rendert,
     weil sich nach Save/Delete payment_saldo+payment_status ändern. -->

<!-- Aus Phase 4 Plan 02 (server.js) -->
GET    /api/invoices/:id/payments     -> 200 + Payment[] | 404
POST   /api/invoices/:id/payments     -> 201 + Payment | 400 (validation) | 403 (Benutzer)
PUT    /api/payments/:id              -> 200 | 400 | 403 | 404
DELETE /api/payments/:id              -> 200 + {success:true,message:'Zahlung geloescht'} | 403 | 404

POST/PUT-Body:
{
  direction: 'in' | 'out',          // PFLICHT bei POST, optional bei PUT
  amount: number,                    // > 0
  payment_date: string,              // 'YYYY-MM-DD'
  payment_method: string,            // optional, default ''
  bank_account_id: number | null,    // null = Bar/Kasse
  reference: string,                 // optional
  notes: string                      // optional
}
NICHT senden: booked_by, created_at — Server ignoriert sie aus Sicherheitsgruenden.

Permission: nur Verwaltung/Buchhaltung/Admin dürfen POST/PUT/DELETE — Header x-user-permission ist bereits durch api()-Funktion gesetzt.

<!-- Bestehende Helper aus app.js -->
function escapeHtml(s)
function formatDate(isoDate)              // 'YYYY-MM-DD' -> 'DD.MM.YYYY'
function showToast(msg, type)             // type='error' für Fehler
function api(path, opts)                  // setzt x-user-name + x-user-permission Header
function canEditInvoice()                 // returns true für Verwaltung/Buchhaltung/Admin
function openModal(title, htmlContent)    // bestehender Modal-Helper -- siehe openNewInvoiceModal Zeile 6270 als Pattern
function closeModal()                     // schließt aktives Modal

<!-- Aus Phase 6 Plan 06-01 wird folgendes benötigt -->
function getPaymentStatusBadge(payment_status)  // Phase 5 Plan 02
function renderInvoicePaymentSaldoHeader(inv)   // Phase 6 Plan 06-01
async function loadInvoicePayments(invoiceId)   // Phase 6 Plan 06-01
async function loadBankAccounts()                // Phase 6 Plan 06-01
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Zahlungstabelle + Modal-Formular + Save/Edit/Delete-Handler implementieren</name>
  <files>public/js/app.js</files>

  <read_first>
    - public/js/app.js Zeilen 6396-6492 (renderInvoiceDetail — Insertion-Punkt für Zahlungs-Block direkt nach Positionen-Card)
    - public/js/app.js Zeilen 6270-6394 (openNewInvoiceModal + saveNewInvoice — Modal-Pattern und Form-Submit als Vorlage)
    - public/js/app.js Zeilen 6494-6515 (renderInvoiceItemsTable — Tabellen-Pattern als Vorlage für Zahlungstabelle)
    - public/js/app.js Zeilen 6543-6552 (deleteInvoice — confirm-Pattern als Vorlage für deleteInvoicePayment)
    - .planning/phases/04-schema-backend/04-02-SUMMARY.md (Body-Validation, Permission-Verhalten)
    - .planning/phases/06-detail-ui-zahlungserfassung/06-01-PLAN.md (welche Funktionen aus Plan 06-01 wiederverwendet werden)
  </read_first>

  <action>
    Drei Editierungen in public/js/app.js. Die fünf neuen Funktionen werden als Block direkt VOR der bestehenden Funktion `function renderInvoiceItemsTable` (Zeile ~6494) eingefügt — also nach `renderInvoiceDetail`'s schließender `}` auf Zeile ~6492.

    ---

    **Änderung A — Funktion `renderInvoicePaymentsBlock(payments, invoice)` einfügen:**

    Erzeugt das komplette HTML für den Zahlungs-Block (Card mit Header + Tabelle + Saldo-Footer). Wird sowohl beim ersten Render als auch bei refreshInvoicePaymentsBlock aufgerufen.

    ```javascript
    // Phase 6 (PAY-UI-01..04): Zahlungs-Block-HTML.
    // payments = Array von Zahlungen (kann leer sein).
    // invoice = aktueller Invoice-Datensatz (fuer Berechtigungs-Check via canEditInvoice + invoice.id fuer onclick-Handler).
    function renderInvoicePaymentsBlock(payments, invoice) {
      const canEdit = canEditInvoice();
      const invoiceId = invoice.id;

      // Saldo-Berechnung fuer Tabellen-Footer (lokal, fuer Anzeige -- Server-Saldo ist source of truth)
      let sumIn = 0, sumOut = 0;
      payments.forEach(p => {
        const amt = Number(p.amount) || 0;
        if (p.direction === 'in') sumIn += amt; else sumOut += amt;
      });
      const saldo = Math.round((sumIn - sumOut) * 100) / 100;

      const headerButtons = canEdit ? `
        <div style="display:flex;gap:8px;">
          <button class="btn btn-sm btn-primary" onclick="openInvoicePaymentForm(${invoiceId}, 'in')">+ Zahlungseingang</button>
          <button class="btn btn-sm btn-secondary" onclick="openInvoicePaymentForm(${invoiceId}, 'out')">+ Zahlungsausgang</button>
        </div>
      ` : '';

      let tableBody = '';
      if (payments.length === 0) {
        const colspan = canEdit ? 8 : 7;
        tableBody = `<tr><td colspan="${colspan}" style="text-align:center;color:var(--text-muted);padding:24px;">Noch keine Zahlungen erfasst.</td></tr>`;
      } else {
        tableBody = payments.map(p => {
          const dir = p.direction === 'in'
            ? '<span class="badge badge-green">Eingang</span>'
            : '<span class="badge badge-orange">Ausgang</span>';
          const konto = p.bank_account_label
            ? escapeHtml(p.bank_account_label)
            : '<span style="color:var(--text-muted);">Bar/Kasse</span>';
          const amountDisplay = (p.direction === 'in' ? '+' : '-') + Number(p.amount).toFixed(2) + ' &euro;';
          const amountColor = p.direction === 'in' ? 'var(--success, #16a34a)' : 'var(--danger, #dc2626)';
          const aktionen = canEdit ? `
            <td style="white-space:nowrap;">
              <button class="btn btn-sm btn-secondary" onclick="openInvoicePaymentForm(${invoiceId}, '${p.direction}', ${p.id})">Bearbeiten</button>
              <button class="btn btn-sm btn-danger" onclick="deleteInvoicePayment(${p.id}, ${invoiceId}, '${p.payment_date}', ${Number(p.amount)})">Löschen</button>
            </td>
          ` : '';
          return `
            <tr>
              <td>${formatDate(p.payment_date)}</td>
              <td>${dir}</td>
              <td style="text-align:right;color:${amountColor};font-weight:600;">${amountDisplay}</td>
              <td>${konto}</td>
              <td>${escapeHtml(p.payment_method || '—')}</td>
              <td>${escapeHtml(p.booked_by || '—')}</td>
              <td>${escapeHtml(p.notes || '')}</td>
              ${aktionen}
            </tr>
          `;
        }).join('');
      }

      const colspanActions = canEdit ? 1 : 0;
      const saldoFooter = `
        <tr style="border-top:2px solid var(--border-color, #e5e7eb);background:var(--bg-subtle, #f9fafb);">
          <td colspan="2" style="text-align:right;font-weight:600;">Saldo (Eingang − Ausgang):</td>
          <td style="text-align:right;font-weight:700;font-size:15px;">${saldo.toFixed(2)} &euro;</td>
          <td colspan="${4 + colspanActions}" style="color:var(--text-muted);font-size:12px;">
            (Eingang: ${sumIn.toFixed(2)} &euro; · Ausgang: ${sumOut.toFixed(2)} &euro;)
          </td>
        </tr>
      `;

      const aktionenTh = canEdit ? '<th style="width:160px;">Aktionen</th>' : '';

      return `
        <div class="card" id="invoice-payments-card">
          <div class="card-header" style="display:flex;justify-content:space-between;align-items:center;">
            <h3>Zahlungen</h3>
            ${headerButtons}
          </div>
          <div class="table-wrapper">
            <table>
              <thead>
                <tr>
                  <th style="width:100px;">Datum</th>
                  <th style="width:90px;">Richtung</th>
                  <th style="width:110px;text-align:right;">Betrag</th>
                  <th>Konto</th>
                  <th style="width:130px;">Zahlungsart</th>
                  <th style="width:120px;">Buchungs-User</th>
                  <th>Notiz</th>
                  ${aktionenTh}
                </tr>
              </thead>
              <tbody>
                ${tableBody}
                ${payments.length > 0 ? saldoFooter : ''}
              </tbody>
            </table>
          </div>
        </div>
      `;
    }
    ```

    ---

    **Änderung B — Funktion `refreshInvoicePaymentsBlock(invoiceId)` einfügen:**

    Re-fetcht Invoice (für aktualisiertes payment_saldo+payment_status) UND Payments-Liste, dann re-rendert sowohl Saldo-Header als auch Zahlungs-Block. Wird nach jedem Save/Delete aufgerufen.

    ```javascript
    // Phase 6: Re-fetcht Invoice + Payments und re-rendert Saldo-Header + Zahlungs-Block.
    // Stellt sicher, dass Saldo-Header und Tabelle nach POST/PUT/DELETE konsistent sind.
    async function refreshInvoicePaymentsBlock(invoiceId) {
      try {
        const [inv, payments] = await Promise.all([
          api(`/api/invoices/${invoiceId}`),
          loadInvoicePayments(invoiceId)
        ]);

        // 1. Saldo-Header neu rendern
        const oldHeader = document.querySelector('.payment-saldo-header');
        if (oldHeader) {
          // Wrapper-Div einfügen mit aktualisiertem HTML
          const wrapper = document.createElement('div');
          wrapper.innerHTML = renderInvoicePaymentSaldoHeader(inv);
          const newHeader = wrapper.firstElementChild;
          if (newHeader) oldHeader.replaceWith(newHeader);
        }

        // 2. Zahlungs-Block neu rendern
        const oldCard = document.getElementById('invoice-payments-card');
        if (oldCard) {
          const wrapper = document.createElement('div');
          wrapper.innerHTML = renderInvoicePaymentsBlock(payments, inv);
          const newCard = wrapper.firstElementChild;
          if (newCard) oldCard.replaceWith(newCard);
        }
      } catch (err) {
        showToast('Aktualisierung fehlgeschlagen: ' + err.message, 'error');
      }
    }
    ```

    ---

    **Änderung C — Funktion `openInvoicePaymentForm(invoiceId, direction, editPaymentId)` einfügen:**

    Öffnet das Modal mit Form. Bei editPaymentId: voraus-gefüllt aus aktueller Tabellen-Daten (via Re-Fetch der Liste, simpler als Tabellen-State zu durchsuchen).

    ```javascript
    // Phase 6 (PAY-UI-02 + PAY-UI-03 + PAY-UI-04): Modal-Formular fuer Zahlungs-Anlage / -Bearbeitung.
    // direction: 'in' oder 'out' -- bestimmt Vorauswahl im Richtungs-Select und Modal-Titel.
    // editPaymentId: optional -- wenn gesetzt, vorausfuellen mit existierender Zahlung (PUT-Modus).
    async function openInvoicePaymentForm(invoiceId, direction, editPaymentId) {
      if (!canEditInvoice()) {
        showToast('Keine Berechtigung', 'error');
        return;
      }

      // Bank-Konten laden (gecached aus Plan 06-01)
      const banks = await loadBankAccounts();

      // Bei Edit: existierende Zahlung holen
      let existing = null;
      if (editPaymentId) {
        try {
          const allPayments = await loadInvoicePayments(invoiceId);
          existing = allPayments.find(p => p.id === editPaymentId);
          if (!existing) {
            showToast('Zahlung nicht gefunden', 'error');
            return;
          }
          direction = existing.direction; // existierende Richtung gewinnt gegen Parameter
        } catch (err) {
          showToast('Fehler beim Laden der Zahlung: ' + err.message, 'error');
          return;
        }
      }

      const today = new Date().toISOString().split('T')[0];
      const defaultDate    = existing ? existing.payment_date     : today;
      const defaultAmount  = existing ? Number(existing.amount).toFixed(2) : '';
      const defaultBankId  = existing ? (existing.bank_account_id || '') : (banks.find(b => b.is_default)?.id || '');
      const defaultMethod  = existing ? existing.payment_method   : 'Überweisung';
      const defaultNotes   = existing ? existing.notes            : '';
      const defaultRef     = existing ? existing.reference        : '';

      const dirLabel = direction === 'in' ? 'Zahlungseingang' : 'Zahlungsausgang';
      const titleAction = existing ? 'bearbeiten' : 'erfassen';

      const bankOptions = `
        <option value="">— Bar / Kasse —</option>
        ${banks.map(b => `
          <option value="${b.id}" ${String(b.id) === String(defaultBankId) ? 'selected' : ''}>
            ${escapeHtml(b.label || b.bank_name || 'Konto')} — ${escapeHtml(b.iban || '')}
          </option>
        `).join('')}
      `;

      const PAYMENT_METHODS = ['Überweisung', 'Bar', 'Kartenzahlung', 'Lastschrift', 'SEPA', 'Sonstige'];
      const methodOptions = PAYMENT_METHODS.map(m =>
        `<option value="${m}" ${m === defaultMethod ? 'selected' : ''}>${m}</option>`
      ).join('');

      openModal(`${dirLabel} ${titleAction}`, `
        <form id="invoice-payment-form" onsubmit="saveInvoicePayment(event, ${invoiceId}, ${editPaymentId || 'null'})">
          <div class="form-row">
            <div class="form-group">
              <label>Datum <span style="color:var(--danger);">*</span></label>
              <input type="date" id="pay-date" value="${defaultDate}" required>
            </div>
            <div class="form-group">
              <label>Richtung <span style="color:var(--danger);">*</span></label>
              <select id="pay-direction" required>
                <option value="in"  ${direction === 'in'  ? 'selected' : ''}>Eingang</option>
                <option value="out" ${direction === 'out' ? 'selected' : ''}>Ausgang</option>
              </select>
            </div>
          </div>
          <div class="form-group">
            <label>Betrag (€) <span style="color:var(--danger);">*</span></label>
            <input type="number" id="pay-amount" step="0.01" min="0.01" value="${defaultAmount}" required placeholder="0.00">
          </div>
          <div class="form-group">
            <label>Bankkonto</label>
            <select id="pay-bank-account">${bankOptions}</select>
            <small style="color:var(--text-muted);">Leer lassen / "Bar" wählen für Kasse-Buchungen.</small>
          </div>
          <div class="form-group">
            <label>Zahlungsart</label>
            <select id="pay-method">${methodOptions}</select>
          </div>
          <div class="form-group">
            <label>Verwendungszweck / Referenz</label>
            <input type="text" id="pay-reference" value="${escapeHtml(defaultRef)}" placeholder="optional">
          </div>
          <div class="form-group">
            <label>Notiz</label>
            <textarea id="pay-notes" rows="2" placeholder="optional">${escapeHtml(defaultNotes)}</textarea>
          </div>
          <div class="form-actions" style="display:flex;justify-content:flex-end;gap:8px;margin-top:16px;">
            <button type="button" class="btn btn-secondary" onclick="closeModal()">Abbrechen</button>
            <button type="submit" class="btn btn-primary">${existing ? 'Speichern' : 'Anlegen'}</button>
          </div>
        </form>
      `);
    }
    ```

    ---

    **Änderung D — Funktion `saveInvoicePayment(event, invoiceId, editPaymentId)` einfügen:**

    Submit-Handler des Forms. Validiert Felder, schickt POST oder PUT, schließt Modal, ruft refreshInvoicePaymentsBlock.

    ```javascript
    // Phase 6: Submit-Handler -- POST bei editPaymentId=null, sonst PUT.
    async function saveInvoicePayment(event, invoiceId, editPaymentId) {
      event.preventDefault();
      if (!canEditInvoice()) { showToast('Keine Berechtigung', 'error'); return; }

      const payment_date = document.getElementById('pay-date').value;
      const direction    = document.getElementById('pay-direction').value;
      const amount       = parseFloat(document.getElementById('pay-amount').value);
      const bankRaw      = document.getElementById('pay-bank-account').value;
      const bank_account_id = bankRaw ? Number(bankRaw) : null;
      const payment_method  = document.getElementById('pay-method').value;
      const reference       = document.getElementById('pay-reference').value.trim();
      const notes           = document.getElementById('pay-notes').value.trim();

      // Frontend-Validierung (Backend macht 400er nochmal sauber)
      if (!payment_date)                  { showToast('Datum ist Pflichtfeld', 'error'); return; }
      if (!['in','out'].includes(direction)) { showToast('Richtung ungueltig', 'error'); return; }
      if (!amount || amount <= 0)         { showToast('Betrag muss > 0 sein', 'error'); return; }

      const body = { direction, amount, payment_date, payment_method, bank_account_id, reference, notes };

      try {
        if (editPaymentId) {
          await api(`/api/payments/${editPaymentId}`, { method: 'PUT', body });
          showToast('Zahlung aktualisiert');
        } else {
          await api(`/api/invoices/${invoiceId}/payments`, { method: 'POST', body });
          showToast(direction === 'in' ? 'Zahlungseingang gebucht' : 'Zahlungsausgang gebucht');
        }
        closeModal();
        await refreshInvoicePaymentsBlock(invoiceId);
      } catch (err) {
        showToast('Fehler: ' + err.message, 'error');
      }
    }
    ```

    ---

    **Änderung E — Funktion `deleteInvoicePayment(paymentId, invoiceId, paymentDate, amount)` einfügen:**

    Confirm + DELETE + refresh.

    ```javascript
    // Phase 6 (PAY-UI-04): Loeschen mit Bestaetigung (Datum + Betrag).
    async function deleteInvoicePayment(paymentId, invoiceId, paymentDate, amount) {
      if (!canEditInvoice()) { showToast('Keine Berechtigung', 'error'); return; }
      const dateStr = formatDate(paymentDate);
      const amtStr = Number(amount).toFixed(2);
      if (!confirm(`Zahlung vom ${dateStr} ueber ${amtStr} EUR wirklich loeschen?`)) return;
      try {
        await api(`/api/payments/${paymentId}`, { method: 'DELETE' });
        showToast('Zahlung geloescht');
        await refreshInvoicePaymentsBlock(invoiceId);
      } catch (err) {
        showToast('Fehler: ' + err.message, 'error');
      }
    }
    ```

    ---

    **Änderung F — renderInvoiceDetail() erweitern: Zahlungs-Block nach Positionen einfügen + Auto-Load:**

    In `renderInvoiceDetail` (Zeile 6397):

    1. Direkt NACH der schließenden `</div>` der Positionen-Card (nach Zeile 6487, also nach `</div>` der invoice-summary-Card-Schließung), folgenden Platzhalter ins Template-Literal einfügen:

       ```html
             <div id="invoice-payments-block-placeholder"></div>
       ```

       So sieht das Template-Ende aus (NACH dem Positionen-Card-Block, VOR dem schließenden Backtick und `;` der `main.innerHTML = ` Zuweisung):

       ```javascript
             <div class="card">
               <div class="card-header">
                 <h3>Positionen</h3>
                 ...
               </div>
               <div id="invoice-items-list">...</div>
               <div class="invoice-summary" id="invoice-summary">...</div>
             </div>

             <div id="invoice-payments-block-placeholder"></div>     // <-- NEU
           `;
       ```

    2. Direkt NACH der `main.innerHTML = ...` Zuweisung (also nach dem schließenden Backtick + `;`), aber NOCH innerhalb des `try`-Blocks, einen Auto-Load einfügen:

       ```javascript
           // Phase 6: Zahlungs-Block laden und rendern (nach initialem Render der Detailseite)
           try {
             const payments = await loadInvoicePayments(id);
             const placeholder = document.getElementById('invoice-payments-block-placeholder');
             if (placeholder) {
               placeholder.outerHTML = renderInvoicePaymentsBlock(payments, inv);
             }
           } catch (paymentsErr) {
             const placeholder = document.getElementById('invoice-payments-block-placeholder');
             if (placeholder) {
               placeholder.outerHTML = `<div class="card"><p style="color:var(--danger);">Zahlungen konnten nicht geladen werden: ${escapeHtml(paymentsErr.message)}</p></div>`;
             }
           }
       ```

       Der innere try/catch verhindert, dass ein Payment-Lade-Fehler den ganzen Detail-Render killt.

    Wichtig:
    - Inline-Position des Zahlungs-Blocks: NACH Positionen-Card. Begründung: Beim ersten Öffnen schaut der Anwender auf Rechnungsposten, dann auf Zahlungen. PDF-Button und Status sind bereits oben.
    - KEIN neues CSS — alles über existierende Klassen `card`, `card-header`, `table-wrapper`, `badge badge-green/orange`, `btn btn-sm btn-*`, plus minimale Inline-Styles für Saldo-Footer.
    - Permission-Check via `canEditInvoice()` — analog zu `canEdit` in renderInvoiceDetail Zeile 6401. Buttons UND Aktionen-Spalte werden nur für Verwaltung/Buchhaltung/Admin gerendert.
    - Nach Save/Delete wird `refreshInvoicePaymentsBlock(invoiceId)` aufgerufen — das re-fetcht Invoice (für aktuellen payment_saldo + payment_status) UND Payments. Saldo-Header und Tabelle aktualisieren sich somit beide ohne Page-Reload.
    - Modal-Form-Submit nutzt onsubmit (statt onclick auf Button) → Browser-native Required-Field-Validierung greift mit.
    - confirm()-Dialog mit Datum (formatDate) und Betrag (toFixed(2)) — entspricht direkter Spec.
  </action>

  <verify>
    <automated>node --check public/js/app.js</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "function renderInvoicePaymentsBlock" public/js/app.js` returns 1
    - `grep -c "async function refreshInvoicePaymentsBlock" public/js/app.js` returns 1
    - `grep -c "async function openInvoicePaymentForm" public/js/app.js` returns 1
    - `grep -c "async function saveInvoicePayment" public/js/app.js` returns 1
    - `grep -c "async function deleteInvoicePayment" public/js/app.js` returns 1
    - `grep -c "+ Zahlungseingang" public/js/app.js` returns ≥ 1
    - `grep -c "+ Zahlungsausgang" public/js/app.js` returns ≥ 1
    - `grep -c "wirklich loeschen" public/js/app.js` returns ≥ 1 (confirm-Dialog mit Datum + Betrag)
    - `grep -c "loadInvoicePayments" public/js/app.js` returns ≥ 3 (definiert in 06-01 + 2 Aufrufe in 06-02: openInvoicePaymentForm + renderInvoiceDetail-Auto-Load)
    - `grep -c "loadBankAccounts" public/js/app.js` returns ≥ 2 (definiert in 06-01 + Aufruf in openInvoicePaymentForm)
    - `grep -c "renderInvoicePaymentSaldoHeader" public/js/app.js` returns ≥ 2 (definiert in 06-01 + Aufruf in refreshInvoicePaymentsBlock)
    - `grep -c "canEditInvoice()" public/js/app.js` returns ≥ 5 (bestehend + 3-4 neue Aufrufe)
    - `grep -c "id=\"invoice-payments-block-placeholder\"" public/js/app.js` returns 1
    - `grep -c "function getPaymentStatusBadge" public/js/app.js` returns 1 (KEIN Duplikat eingeführt)
    - `node --check public/js/app.js` exit 0
  </acceptance_criteria>

  <done>
    Alle 5 neuen Funktionen vorhanden und syntaktisch valide, renderInvoiceDetail bindet den Block ein, Auto-Load + Permission-Check + Refresh-Logik konsistent.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-End Browser-Verifikation der Zahlungs-Detail-UI</name>

  <files>(keine Aenderung — User-Verifikation, optionale Auto-Fix-Round bei beobachteten Issues kann public/js/app.js anfassen)</files>

  <read_first>
    - .planning/phases/05-status-logik-listen/05-02-SUMMARY.md (Verification Notes — voller 5-Status-Übergangs-E2E wurde explizit auf Phase 6 verschoben)
    - .planning/phases/05-status-logik-listen/05-01-SUMMARY.md (E2E-Verifikation mit Test-Rechnung 9, Bezugswerte payment_saldo/payment_status)
  </read_first>

  <what-built>
    Komplette interaktive Zahlungserfassung in der Rechnungs-Detailseite:
    - Saldo+Status-Header oben (aus Plan 06-01)
    - Zahlungs-Tabelle nach Positionen-Card (chronologisch, mit Saldo-Footer)
    - "+ Zahlungseingang" / "+ Zahlungsausgang" Buttons (nur Verwaltung/Buchhaltung/Admin)
    - Modal-Formular (Datum / Richtung / Betrag / Bankkonto / Zahlungsart / Referenz / Notiz)
    - Inline Bearbeiten / Löschen pro Zeile mit confirm-Dialog
    - Auto-Refresh von Header + Tabelle nach Save/Delete (ohne Page-Reload)
  </what-built>

  <how-to-verify>
    **Setup:**
    1. Server ist nach Backend-Änderungen neuzustarten (gemäß MEMORY-Pattern). Da Plan 06-02 nur Frontend-Änderungen hat, reicht: Browser-Hard-Reload (Strg+F5) auf der Bemo-Seite.
    2. Im Browser eingeloggt als Verwaltung/Buchhaltung/Admin.

    **Verifikations-Sequenz (5-Status-Übergang, der laut 05-02-SUMMARY.md auf diese Phase verschoben wurde):**

    **A. Setup-Rechnung anlegen:**
    1. Neue Rechnung mit einem Testkunden anlegen.
    2. Eine Position hinzufügen mit Bezeichnung "Test", Menge=1, Preis=1000 EUR Netto.
       Achtung: Der pre-existing vat_rate-Bug (siehe 05-01-SUMMARY.md) führt zu total_gross=1190 statt 1000. Das ist OK — die Status-Übergänge skalieren mit dem realen Brutto-Wert.
    3. Auf die Detailseite der Rechnung navigieren.

    **B. Initial-State:**
    - [ ] Saldo-Header oben zeigt: Status-Badge "Offen" (grau), "Bereits bezahlt: 0.00 €", "Offener Betrag: 1190.00 €" (rot)
    - [ ] Zahlungs-Block unten zeigt: "Noch keine Zahlungen erfasst."
    - [ ] Buttons "+ Zahlungseingang" und "+ Zahlungsausgang" sind sichtbar (canEditInvoice=true)

    **C. Eingang 400 EUR — Status soll "Teilbezahlt" werden:**
    1. Klick auf "+ Zahlungseingang" → Modal öffnet sich, Titel "Zahlungseingang erfassen", Datum=heute, Richtung=Eingang vorausgewählt.
    2. Bankkonto-Dropdown ist mit Werten gefüllt (Default-Konto vorausgewählt) oder zeigt "Bar/Kasse".
    3. Betrag=400, Zahlungsart=Überweisung, Notiz="Test 1", "Anlegen" klicken.
    - [ ] Modal schließt sich, Toast "Zahlungseingang gebucht".
    - [ ] Saldo-Header aktualisiert sich OHNE Page-Reload: Status-Badge "Teilbezahlt" (orange), "Bereits bezahlt: 400.00 €", "Offener Betrag: 790.00 €".
    - [ ] Zahlungs-Tabelle zeigt 1 Zeile: Datum / Eingang (grün) / +400.00 € / Konto-Label / Überweisung / [User] / Test 1 / [Aktionen].
    - [ ] Tabellen-Saldo-Footer: "Saldo: 400.00 €" und "(Eingang: 400.00 € · Ausgang: 0.00 €)".

    **D. Eingang 790 EUR — Status soll "Bezahlt" werden:**
    1. "+ Zahlungseingang" → Betrag=790 → "Anlegen".
    - [ ] Saldo-Header: Status-Badge "Bezahlt" (grün), "Bereits bezahlt: 1190.00 €", "Offener Betrag: 0.00 €" (grün).
    - [ ] Tabelle: 2 Zeilen, Footer-Saldo: 1190.00 €.

    **E. Eingang 100 EUR — Status soll "Ueberzahlt" werden:**
    1. "+ Zahlungseingang" → Betrag=100 → "Anlegen".
    - [ ] Saldo-Header: Status-Badge "Ueberzahlt" (blau), "Bereits bezahlt: 1290.00 €", "Offener Betrag: -100.00 €" (dezent grau, mit "(ueberzahlt)" Hinweis).

    **F. Ausgang 100 EUR — Status soll wieder "Bezahlt" werden:**
    1. "+ Zahlungsausgang" → Modal öffnet sich, Richtung=Ausgang vorausgewählt → Betrag=100 → "Anlegen".
    - [ ] Saldo-Header: Status-Badge "Bezahlt" (grün), "Bereits bezahlt: 1190.00 €", "Offener Betrag: 0.00 €".
    - [ ] Tabelle: 4 Zeilen, ein "Ausgang" mit -100.00 € (rot/orange).

    **G. Bearbeiten — Test:**
    1. In einer Eingangs-Zeile auf "Bearbeiten" klicken → Modal öffnet sich vorausgefüllt mit den Werten der Zeile.
    2. Betrag um z.B. 50 EUR ändern → "Speichern".
    - [ ] Saldo-Header passt sich entsprechend an.
    - [ ] Tabelle zeigt aktualisierten Wert.

    **H. Löschen — Test:**
    1. In einer Zeile auf "Löschen" klicken.
    - [ ] confirm()-Dialog erscheint mit Text wie "Zahlung vom 28.04.2026 ueber 100.00 EUR wirklich loeschen?".
    2. "OK" → Zahlung verschwindet, Tabelle und Saldo aktualisieren sich.

    **I. Permission-Check (optional, falls leicht testbar):**
    1. Mit Rolle "Benutzer" einloggen.
    2. Detailseite derselben Rechnung öffnen.
    - [ ] Saldo-Header sichtbar (Lese-Zugriff).
    - [ ] Zahlungs-Tabelle sichtbar mit allen Zeilen.
    - [ ] "+ Zahlungseingang" / "+ Zahlungsausgang" Buttons NICHT sichtbar.
    - [ ] "Bearbeiten" / "Löschen" Buttons in den Zeilen NICHT sichtbar.

    **J. Konsistenz-Check Liste ↔ Detail:**
    1. Zur Rechnungs-Liste navigieren.
    - [ ] Phase-5-Spalte "Zahlung" zeigt für die Test-Rechnung denselben Status wie der Detail-Header.

    **Bei Auffälligkeiten:**
    - Modal öffnet nicht / Form lässt sich nicht abschicken / Werte aktualisieren nicht: Browser-Console-Error notieren und in Resume-Signal beschreiben — Auto-Fix-Round bringt das in Ordnung.
    - Optisch unschön (Spaltenbreiten, Farben, Position): wenn nur Polish nötig, "approved" möglich mit Hinweis auf späteres CSS-Polish.
  </how-to-verify>

  <acceptance_criteria>
    - Status-Übergänge offen → teilbezahlt → bezahlt → ueberzahlt → bezahlt visuell und numerisch korrekt durchgespielt (B-F)
    - Bearbeiten füllt Form korrekt aus, PUT funktioniert (G)
    - Löschen zeigt korrekten confirm-Dialog mit Datum + Betrag und entfernt Zahlung (H)
    - Permission-Check: Buttons NUR für Verwaltung/Buchhaltung/Admin sichtbar (I)
    - Detail-Status-Badge stimmt mit Liste-Status-Badge überein (J)
    - Saldo-Header und Tabelle aktualisieren OHNE Page-Reload nach jedem POST/PUT/DELETE
    - User schreibt "approved" oder beschreibt Issues
  </acceptance_criteria>

  <action>
    User durchlaeuft die Verifikations-Sequenz aus <how-to-verify> (Punkte A-J) im Browser:
    1. Test-Rechnung anlegen (Brutto ca. 1190 EUR wegen vat_rate-Bug)
    2. Initial-State pruefen (Status "Offen", Tabelle leer)
    3. 5-Status-Uebergangs-Sequenz: +400 (teilbezahlt) -> +790 (bezahlt) -> +100 (ueberzahlt) -> -100 (bezahlt)
    4. Bearbeiten + Loeschen testen
    5. Permission-Check mit Benutzer-Rolle
    6. Konsistenz-Check Liste <-> Detail
    Bei Issues: Browser-Console-Error notieren -> Auto-Fix-Round in Plan 06-02 verlaengert (analog zu Phase 5 Plan 02 Pattern).
  </action>

  <verify>
    <automated>node --check public/js/app.js</automated>
    Manuelle visuelle Verifikation laut <how-to-verify>: 10 Pruefpunkte (A-J).
  </verify>

  <done>
    User schreibt "approved" — alle 10 Verifikations-Punkte erfolgreich durchgespielt; Saldo+Status-Header und Zahlungs-Tabelle aktualisieren OHNE Page-Reload nach jedem POST/PUT/DELETE; Permission-Check verhindert Schreib-Aktionen fuer Benutzer-Rolle.
  </done>

  <resume-signal>
    User schreibt "approved" wenn alle 10 Verifikations-Punkte (A-J) korrekt sind.
    User beschreibt Issues konkret falls etwas nicht funktioniert (Browser-Console-Error oder Beschreibung des falschen Verhaltens) — Auto-Fix-Round bringt das in Ordnung.
  </resume-signal>
</task>

</tasks>

<verification>
**Statische Checks (automatisiert, nach Task 1):**
1. `node --check public/js/app.js` exit 0
2. Alle 5 neuen Funktionen vorhanden (renderInvoicePaymentsBlock, refreshInvoicePaymentsBlock, openInvoicePaymentForm, saveInvoicePayment, deleteInvoicePayment)
3. Wiederverwendung verifiziert: getPaymentStatusBadge 1× definiert, loadInvoicePayments + loadBankAccounts + renderInvoicePaymentSaldoHeader von Plan 06-01 importiert (Funktions-Aufrufe gegrept)
4. Permission-Check via canEditInvoice() überall vorhanden (Buttons + Aktionen-Spalte + saveInvoicePayment + deleteInvoicePayment)
5. confirm-Dialog enthält Datum + Betrag

**Dynamische Verifikation (Task 2 Checkpoint):**
- Volle 5-Status-Übergangs-Sequenz (offen→teilbezahlt→bezahlt→ueberzahlt→bezahlt) live durchgespielt
- Bearbeiten + Löschen + Permission-Check + Konsistenz-Check Liste↔Detail bestätigt
</verification>

<success_criteria>
Aus ROADMAP Phase 6 (alle 5 Success-Kriterien dieser Phase werden hier abgenommen — PAY-STAT-04 + PAY-UI-05 sind bereits durch Plan 06-01 abgedeckt, Plan 06-02 deckt PAY-UI-01..04 ab UND verifiziert die Gesamt-Phase im Checkpoint):

1. ✓ "Zahlungen"-Block mit chronologischer Tabelle und allen 7 Spalten — PAY-UI-01
2. ✓ "+ Zahlungseingang" / "+ Zahlungsausgang" Buttons öffnen Formular mit allen Pflichtfeldern, Speichern aktualisiert ohne Reload — PAY-UI-02 + PAY-UI-03
3. ✓ Bearbeiten und Löschen pro Zeile mit Bestätigungs-Dialog, beide aktualisieren Tabelle und Saldo — PAY-UI-04
4. ✓ Saldo-Anzeige (Bereits-bezahlt + Offener-Betrag + Status-Badge) konsistent mit Phase 5 Backend — bereits durch Plan 06-01 erfüllt, hier durch Refresh-Logik nach Save/Delete bestätigt — PAY-UI-05
5. ✓ Status-Badge prominent oben — bereits durch Plan 06-01 erfüllt — PAY-STAT-04
</success_criteria>

<output>
Nach Abschluss erstelle `.planning/phases/06-detail-ui-zahlungserfassung/06-02-SUMMARY.md` gemäß Template (siehe summary.md). Pflicht-Felder im Frontmatter:
- requirements-completed: [PAY-UI-01, PAY-UI-02, PAY-UI-03, PAY-UI-04]
- provides: 5 neue Funktionen + Auto-Load in renderInvoiceDetail + voller E2E-Approval
- affects: [milestone-v1.1-complete]

In SUMMARY.md "Verification Notes" festhalten, wie der A-J-Verifikationslauf konkret abgelaufen ist (welche Werte beobachtet, welche Spalten geprüft) — analog zur Auflistung in 05-02-SUMMARY.md "Verification Notes (Checkpoint Task 2)".
</output>
