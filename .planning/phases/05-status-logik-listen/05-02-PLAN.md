---
phase: 05-status-logik-listen
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - public/js/app.js
autonomous: false
requirements:
  - PAY-STAT-03

must_haves:
  truths:
    - "In der Rechnungs-Liste erscheint pro Rechnung ein Zahlungsstatus-Badge (offen/teilbezahlt/bezahlt/ueberzahlt) basierend auf payment_status aus dem Backend"
    - "Der bestehende invoices.status (Entwurf/Versendet/...) bleibt sichtbar — payment_status wird als ZUSAETZLICHE Spalte/Badge angezeigt, nicht als Ersatz"
    - "Badge-Farben: offen=grau, teilbezahlt=orange, bezahlt=gruen, ueberzahlt=blau (Plan-Entscheidung — blau statt rot, weil Ueberzahlung ein Information-State, kein Fehler-State ist)"
    - "Anzeige-Strings sind kapitalisiert mit Umlaut-sicherer Schreibung: 'Offen', 'Teilbezahlt', 'Bezahlt', 'Ueberzahlt' (Backend liefert lowercase ohne Umlaut, Frontend mapped fuer Display)"
    - "Eine Zahlung die in Phase 4 angelegt wurde, veraendert den Badge in der Liste sofort beim naechsten Reload (kein manueller Status-Eingriff noetig)"
    - "User-Verifikation: Visuell + funktional bestaetigt — Liste zeigt korrekte Badges fuer ein End-to-End-Test-Szenario"
  artifacts:
    - path: "public/js/app.js"
      provides: "getPaymentStatusBadge(payment_status) Helper-Funktion"
      contains: "function getPaymentStatusBadge"
    - path: "public/js/app.js"
      provides: "Erweiterte Tabellen-Spalte 'Zahlung' in renderInvoices()/applyInvoiceFilters() mit dem neuen Badge"
      contains: "payment_status"
  key_links:
    - from: "applyInvoiceFilters() Tabellen-Render"
      to: "getPaymentStatusBadge(inv.payment_status)"
      via: "Inline-Aufruf in der TR-Map des tbody-innerHTML"
      pattern: "getPaymentStatusBadge\\(inv\\.payment_status\\)"
    - from: "Tabellen-Header"
      to: "Tabellen-Body Spalten"
      via: "colspan=9 statt 8 in der empty-state Row, neue <th>Zahlung</th>, neue <td> in jeder TR"
      pattern: "colspan=\"9\""
    - from: "Badge-CSS"
      to: "Existing badge-* classes (style.css)"
      via: "Wiederverwendung von .badge-gray, .badge-orange, .badge-green, .badge-blue"
      pattern: "badge-(gray|orange|green|blue)"
---

<objective>
Frontend Zahlungsstatus-Badge in der Rechnungs-Liste anzeigen.

Purpose: Der in Plan 05-01 abgeleitete payment_status ist nun in der API verfuegbar, soll aber fuer den Anwender direkt in der Rechnungs-Liste sichtbar sein. Eine neue Spalte "Zahlung" mit farbigem Badge zeigt sofort, welche Rechnungen offen, teilweise bezahlt, vollstaendig bezahlt oder ueberbezahlt sind — ohne dass der Anwender eine Detailseite oeffnen muss.

Output: getPaymentStatusBadge()-Helper + erweiterte Tabelle in renderInvoices()/applyInvoiceFilters() mit neuer Spalte "Zahlung". Filter, Sortierung und alle bestehenden Spalten bleiben unveraendert.
</objective>

<execution_context>
@C:/Users/BenAlbers/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/BenAlbers/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-status-logik-listen/05-01-PLAN.md
@public/js/app.js
@public/css/style.css

<interfaces>
<!-- Backend-Output (aus Plan 05-01) und bestehende Frontend-Pattern. -->

Backend-Response /api/invoices (nach Plan 05-01):
```typescript
type InvoiceListRow = {
  id: number;
  invoice_number: string;
  invoice_date: string;
  customer_name: string;
  payment_method: string;
  total_net: number;
  total_gross: number;
  status: string;          // bestehender manueller Status: Entwurf/Offen/Bezahlt/Storniert/Mahnstufe 1/2
  payment_saldo: number;   // NEU: SUM(in) - SUM(out), 2 Nachkommastellen
  payment_status: string;  // NEU: 'offen' | 'teilbezahlt' | 'bezahlt' | 'ueberzahlt' (lowercase, ohne Umlaut)
  // ... weitere bestehende Felder
};
```

Bestehender getInvoiceStatusBadge (app.js Zeile 6096):
```javascript
function getInvoiceStatusBadge(status) {
  const map = {
    'Entwurf':     'gray',
    'Offen':       'blue',
    'Bezahlt':     'green',
    'Storniert':   'red',
    'Mahnstufe 1': 'mahnstufe1',
    'Mahnstufe 2': 'mahnstufe2',
  };
  return `<span class="badge badge-${map[status] || 'gray'}">${escapeHtml(status)}</span>`;
}
```

Vorhandene Badge-CSS-Klassen (style.css Zeilen 800-849) — alle bereits vorhanden, KEIN neues CSS noetig:
- .badge — display:inline-block, padding:2px 8px, border-radius:9999px, font-size:11px, font-weight:600, text-transform:uppercase
- .badge-gray   — background:#f3f4f6, color:#374151
- .badge-blue   — background:#dbeafe, color:#1e40af
- .badge-green  — background:#d1fae5, color:#065f46
- .badge-orange — background:#fff7ed, color:#c2410c
- .badge-red    — background:#dc2626, color:#ffffff (nicht verwendet — siehe Decision)

Bestehende renderInvoices (app.js Zeile 6111):
- Render-Phase: einmaliges Layout mit `<thead>` (8 Spalten) + leerer `<tbody id="invoices-tbody">`
- Filter-Phase: applyInvoiceFilters() liest Inputs, filtert _allInvoices, schreibt tbody.innerHTML
- TR-Template enthaelt 8 `<td>`-Zellen, getInvoiceStatusBadge(inv.status) in Spalte 7
- Empty-State: `<tr><td colspan="8">Keine Rechnungen gefunden</td></tr>`

Aktuelle Spaltenstruktur:
1. Rechnungsnr. | 2. Datum | 3. Kundenname | 4. Zahlart | 5. Netto | 6. Brutto | 7. Status | 8. Aktionen

Neue Spaltenstruktur (nach diesem Plan):
1. Rechnungsnr. | 2. Datum | 3. Kundenname | 4. Zahlart | 5. Netto | 6. Brutto | 7. Status | 8. **Zahlung** (NEU) | 9. Aktionen
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: getPaymentStatusBadge()-Helper hinzufuegen + Tabelle in renderInvoices()/applyInvoiceFilters() um Spalte 'Zahlung' erweitern</name>

  <files>public/js/app.js</files>

  <read_first>
    - public/js/app.js Zeilen 6092-6228 (kompletter Invoices-Block: Konstanten, getInvoiceStatusBadge, renderInvoices, applyInvoiceFilters)
    - public/js/app.js Zeile 8695 ff. (FleetInsurance-Badges als Inline-Style-Referenz — wird nicht verwendet, getInvoiceStatusBadge mit class="badge badge-X" ist konsistenter)
    - public/css/style.css Zeilen 800-849 (Badge-Klassen — KEINE Aenderung, nur Wiederverwendung)
  </read_first>

  <action>
    DREI Aenderungen in public/js/app.js:

    **1. Neue Helper-Funktion direkt NACH `function getInvoiceStatusBadge(...)` einfuegen (ca. Zeile 6107).**

    Genauer Code:
    ```javascript
    // Phase 5 (PAY-STAT-03): Zahlungsstatus-Badge fuer abgeleiteten payment_status aus /api/invoices
    // Backend liefert lowercase ohne Umlaut: 'offen' | 'teilbezahlt' | 'bezahlt' | 'ueberzahlt'
    // Frontend zeigt kapitalisiert.
    function getPaymentStatusBadge(payment_status) {
      const map = {
        'offen':       { color: 'gray',   label: 'Offen' },
        'teilbezahlt': { color: 'orange', label: 'Teilbezahlt' },
        'bezahlt':     { color: 'green',  label: 'Bezahlt' },
        'ueberzahlt':  { color: 'blue',   label: 'Ueberzahlt' },
      };
      const entry = map[payment_status] || { color: 'gray', label: payment_status || 'Unbekannt' };
      return `<span class="badge badge-${entry.color}">${escapeHtml(entry.label)}</span>`;
    }
    ```

    Begruendung der Farben (Plan-Entscheidung):
    - offen=grau (neutral, kein Druck)
    - teilbezahlt=orange (Aufmerksamkeit, Aktion noch offen)
    - bezahlt=gruen (positives "Erledigt"-Signal)
    - ueberzahlt=blau (Info-State, kein Fehler — eine Ueberzahlung ist meist ein bewusstes Vorauszahlen oder Skontoausgleich, nicht falsch)

    Begruendung der Anzeige-Strings: Kapitalisiert mit deutschem 'Ue' statt 'Ü' (HTML-sicher in allen Browsern, konsistent mit lowercase Backend-Identifier).

    **2. In renderInvoices() (Zeile ~6122-6161) die `<thead>`-Tabellenstruktur erweitern: neue Spalte 'Zahlung' VOR 'Aktionen' einfuegen.**

    Bestehender Block (Zeilen ~6131-6155):
    ```html
    <thead>
      <tr>
        <th>Rechnungsnr.</th>
        <th>Datum</th>
        <th>Kundenname</th>
        <th>Zahlart</th>
        <th>Netto</th>
        <th>Brutto</th>
        <th>Status</th>
        <th>Aktionen</th>
      </tr>
      <tr class="filter-row">
        <td><input type="text" id="inv-filter-nr"       ...></td>
        <td><input type="text" id="inv-filter-date"     ...></td>
        <td><input type="text" id="inv-filter-customer" ...></td>
        <td><input type="text" id="inv-filter-zahlart"  ...></td>
        <td></td>
        <td></td>
        <td>
          <select id="inv-filter-status" ...>...</select>
        </td>
        <td></td>
      </tr>
    </thead>
    ```

    Neuer Block (genau ersetzen):
    ```html
    <thead>
      <tr>
        <th>Rechnungsnr.</th>
        <th>Datum</th>
        <th>Kundenname</th>
        <th>Zahlart</th>
        <th>Netto</th>
        <th>Brutto</th>
        <th>Status</th>
        <th>Zahlung</th>
        <th>Aktionen</th>
      </tr>
      <tr class="filter-row">
        <td><input type="text" id="inv-filter-nr"       placeholder="Suchen..." oninput="applyInvoiceFilters()" class="filter-input"></td>
        <td><input type="text" id="inv-filter-date"     placeholder="z.B. 03.2026" value="${defaultDateFilter}" oninput="applyInvoiceFilters()" class="filter-input"></td>
        <td><input type="text" id="inv-filter-customer" placeholder="Suchen..." oninput="applyInvoiceFilters()" class="filter-input"></td>
        <td><input type="text" id="inv-filter-zahlart"  placeholder="Suchen..." oninput="applyInvoiceFilters()" class="filter-input"></td>
        <td></td>
        <td></td>
        <td>
          <select id="inv-filter-status" onchange="applyInvoiceFilters()" class="filter-input">
            <option value="">Alle</option>
            ${INVOICE_STATUSES.map(s => `<option value="${s}">${s}</option>`).join('')}
          </select>
        </td>
        <td>
          <select id="inv-filter-payment-status" onchange="applyInvoiceFilters()" class="filter-input">
            <option value="">Alle</option>
            <option value="offen">Offen</option>
            <option value="teilbezahlt">Teilbezahlt</option>
            <option value="bezahlt">Bezahlt</option>
            <option value="ueberzahlt">Ueberzahlt</option>
          </select>
        </td>
        <td></td>
      </tr>
    </thead>
    ```

    **3. In applyInvoiceFilters() (Zeile ~6169-6227) drei Aenderungen:**

    a) NEUER Filter-Reader oben in der Funktion, NACH der bestehenden `status`-Zeile (~Zeile 6174):
    ```javascript
    const paymentStatus = (document.getElementById('inv-filter-payment-status')?.value || '');
    ```

    b) NEUE Filter-Klausel in der `_allInvoices.filter(...)`-Lambda, NACH der bestehenden `status`-Klausel (~Zeile 6200):
    ```javascript
    if (paymentStatus && inv.payment_status !== paymentStatus) return false;
    ```

    c) Empty-State und TR-Template erweitern.
    Empty-State (Zeile ~6208) `colspan="8"` -> `colspan="9"`:
    ```javascript
    tbody.innerHTML = `<tr><td colspan="9" style="text-align:center;color:var(--text-muted);padding:24px;">Keine Rechnungen gefunden.</td></tr>`;
    ```

    TR-Template (Zeile ~6212-6226): NEUE `<td>` mit Badge VOR der Aktionen-Spalte:
    ```javascript
    tbody.innerHTML = filtered.map(inv => `
      <tr class="clickable" ondblclick="navigate('invoice-detail', ${inv.id})">
        <td><strong>${escapeHtml(inv.invoice_number)}</strong></td>
        <td>${formatDate(inv.invoice_date)}</td>
        <td>${escapeHtml(inv.customer_name || '')}</td>
        <td>${escapeHtml(inv.payment_method || '')}</td>
        <td>${Number(inv.total_net).toFixed(2)} &euro;</td>
        <td>${Number(inv.total_gross).toFixed(2)} &euro;</td>
        <td>${getInvoiceStatusBadge(inv.status)}</td>
        <td>${getPaymentStatusBadge(inv.payment_status)}</td>
        <td style="white-space:nowrap;">
          <button class="btn btn-sm btn-secondary" onclick="event.stopPropagation(); navigate('invoice-detail', ${inv.id})">Öffnen</button>
          <button class="btn btn-sm btn-primary" onclick="event.stopPropagation(); window.open('/api/invoices/${inv.id}/pdf','_blank')">PDF</button>
        </td>
      </tr>
    `).join('');
    ```

    WICHTIG:
    - KEIN neues CSS noetig — alle 4 Badge-Farben (gray, orange, green, blue) existieren bereits in style.css.
    - Bestehender `getInvoiceStatusBadge(inv.status)` bleibt unveraendert in der Status-Spalte — der manuelle Rechnungs-Status (Entwurf/Versendet/Mahnstufe...) ist weiterhin sichtbar.
    - Filter "Zahlung" ist additiv — User kann gleichzeitig nach manuellem status und nach payment_status filtern.
    - Falls der Backend-Response `payment_status` mal `undefined` liefert (z.B. waehrend Plan 05-01 noch deployed wird), zeigt der Helper "Unbekannt" als Default-Label.
  </action>

  <verify>
    <automated>
      # Statisch
      grep -n "function getPaymentStatusBadge" public/js/app.js \
        && grep -n "'teilbezahlt'" public/js/app.js \
        && grep -n "'ueberzahlt'" public/js/app.js \
        && grep -n "getPaymentStatusBadge(inv.payment_status)" public/js/app.js \
        && grep -n "<th>Zahlung</th>" public/js/app.js \
        && grep -n "inv-filter-payment-status" public/js/app.js \
        && grep -n "colspan=\"9\"" public/js/app.js \
        && node --check public/js/app.js

      # Dynamisch (Browser-loses Smoke via Headless-Eval):
      node -e "const fs=require('fs'); const code=fs.readFileSync('public/js/app.js','utf8');" \
            -e "const fnMatch = code.match(/function getPaymentStatusBadge\\([\\s\\S]*?\\n\\}/);" \
            -e "if(!fnMatch){console.error('FAIL: getPaymentStatusBadge not found'); process.exit(1);}" \
            -e "const escapeHtml = s => String(s||'').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/&/g,'&amp;');" \
            -e "eval(fnMatch[0]);" \
            -e "const tests = [['offen','Offen','gray'],['teilbezahlt','Teilbezahlt','orange'],['bezahlt','Bezahlt','green'],['ueberzahlt','Ueberzahlt','blue'],['unknown','unknown','gray']];" \
            -e "for(const [input,labelExp,colorExp] of tests){const html=getPaymentStatusBadge(input); if(!html.includes('badge-'+colorExp)){console.error('FAIL color',input,'->',html); process.exit(1);} if(!html.includes(labelExp)){console.error('FAIL label',input,'->',html); process.exit(1);}}" \
            -e "console.log('OK 5/5 badge variants');"
    </automated>
  </verify>

  <acceptance_criteria>
    - public/js/app.js enthaelt exakten String "function getPaymentStatusBadge"
    - public/js/app.js enthaelt alle 4 Status-Mappings: 'offen', 'teilbezahlt', 'bezahlt', 'ueberzahlt'
    - public/js/app.js enthaelt im Tabellen-Header neue Zeile `<th>Zahlung</th>`
    - public/js/app.js enthaelt im Tabellen-Body Aufruf `getPaymentStatusBadge(inv.payment_status)`
    - public/js/app.js enthaelt Filter-Element `id="inv-filter-payment-status"` mit 4 Optionen + "Alle"
    - public/js/app.js Empty-State Row enthaelt `colspan="9"` (NICHT mehr 8)
    - `node --check public/js/app.js` -> exit 0
    - Inline-Node-Eval: getPaymentStatusBadge() liefert fuer alle 4 valid-Inputs + 1 unbekannt-Input die richtige Farbe und das richtige Label
    - `getInvoiceStatusBadge(inv.status)` (Spalte 7) UNVERAENDERT vorhanden (bestehende Status-Spalte intakt)
  </acceptance_criteria>

  <done>
    Die Rechnungs-Liste hat eine neue Spalte 'Zahlung' mit farbigem Badge (gray/orange/green/blue) basierend auf payment_status aus dem Backend. Filter fuer payment_status existiert. Bestehender invoices.status bleibt unangetastet. Keine CSS-Aenderungen.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visuelle und funktionale Verifikation der Zahlungsstatus-Spalte</name>

  <files>(keine Aenderung — User-Verifikation)</files>

  <read_first>
    - .planning/ROADMAP.md Phase 5 Success Criteria #3 und #4 (Test-Szenario Brutto 1000 EUR)
  </read_first>

  <what-built>
    - Backend (Plan 05-01): GET /api/invoices und GET /api/invoices/:id liefern jetzt payment_saldo (number) und payment_status (string) pro Rechnung
    - Frontend (dieser Plan, Task 1): Neue Tabellen-Spalte 'Zahlung' mit Status-Badge (Offen/Teilbezahlt/Bezahlt/Ueberzahlt), neuer Dropdown-Filter, KEIN CSS geaendert
    - getPaymentStatusBadge()-Helper als Pendant zum bestehenden getInvoiceStatusBadge()
  </what-built>

  <how-to-verify>
    **Voraussetzung:** Server laeuft mit den neuen Endpoints aus Plan 05-01. Falls Server nach den Backend-Aenderungen nicht neu gestartet wurde -> Server selbst neustarten (Memory-Hinweis: "Server nach Backend-Aenderungen immer selbst neustarten").

    **End-to-End-Szenario aus ROADMAP.md Phase 5 Success Criterion #4:**

    1. Browser oeffnen unter http://localhost:3001/, einloggen mit Verwaltung-/Buchhaltung-/Admin-Rolle
    2. Zur Seite "Rechnungen" navigieren
    3. **Pruefung 1 (Layout):** Tabellen-Header zeigt 9 Spalten — speziell muss zwischen 'Status' und 'Aktionen' eine neue Spalte 'Zahlung' sichtbar sein
    4. **Pruefung 2 (Filter):** Unter dem 'Zahlung'-Header ist ein Dropdown 'Alle/Offen/Teilbezahlt/Bezahlt/Ueberzahlt' vorhanden
    5. **Pruefung 3 (Badge fuer alte Rechnungen):** Bestehende Rechnungen ohne Zahlungen zeigen einen grauen 'Offen'-Badge
    6. **End-to-End-Test mit neuer Rechnung:**
       a. Neue Rechnung anlegen (Kunde waehlen, Brutto-Position 1000 EUR ergaenzen — z.B. qty=1, unit_price=1000, vat_rate=0)
       b. Zur Liste zurueck -> Badge ist grauer **'Offen'**
       c. Detailseite oeffnen, dann via API-Aufruf oder Phase-6-UI eine Zahlung anlegen — alternativ via curl:
          ```
          curl -X POST http://localhost:3001/api/invoices/<INV_ID>/payments \
               -H "x-user-permission: Verwaltung" -H "x-user-name: <dein-name>" -H "Content-Type: application/json" \
               -d '{"direction":"in","amount":400,"payment_date":"2026-04-28","payment_method":"Ueberweisung"}'
          ```
       d. Liste neuladen -> Badge ist orange **'Teilbezahlt'**
       e. Weitere Zahlung 600 EUR Eingang anlegen -> Liste neu -> Badge ist gruen **'Bezahlt'**
       f. Noch eine Zahlung 100 EUR Eingang anlegen -> Liste neu -> Badge ist blau **'Ueberzahlt'**
       g. Eine Zahlung 100 EUR Ausgang anlegen -> Liste neu -> Badge ist gruen **'Bezahlt'**
    7. **Pruefung 4 (Filter funktioniert):** Filter 'Zahlung' auf 'Bezahlt' setzen -> nur die Test-Rechnung (und ggf. andere bezahlte) sind sichtbar
    8. **Pruefung 5 (Bestehender Status unangetastet):** Spalte 'Status' (vor 'Zahlung') zeigt weiterhin 'Entwurf' fuer die Test-Rechnung — der bestehende manuelle Status ist UNVERAENDERT
    9. **Cleanup:** Test-Rechnung loeschen (DELETE durch Admin moeglich solange status='Entwurf' — Cascade entfernt invoice_payments-Eintraege automatisch)

    **Erwartetes Ergebnis:**
    - 5 Status-Wechsel des Badges in der korrekten Sequenz
    - Filter funktioniert
    - Layout sauber (keine ueberlappenden Spalten, Badge gut lesbar)
    - Bestehender Rechnungs-Status separat sichtbar
  </how-to-verify>

  <action>
    Pausieren und auf User-Verifikation warten. KEINE automatisierten Code-Aenderungen.
    Voraussetzung: Task 1 (Frontend-Code) ist gemerged und Server laeuft mit den Backend-Aenderungen aus Plan 05-01.
    User durchlaeuft die End-to-End-Schritte 1-9 aus dem Block <how-to-verify> oben und meldet das Ergebnis zurueck via resume-signal.
  </action>

  <verify>
    <automated>echo "MANUAL CHECKPOINT — wartet auf User-Approval. Verifikations-Schritte stehen im <how-to-verify> Block."</automated>
  </verify>

  <acceptance_criteria>
    - User hat alle 5 Status-Uebergaenge live durchgespielt und visuell bestaetigt
    - User signalisiert "approved" via resume-signal
    - Kein Issue-Report fuer Layout, Filter, Badge-Farbe oder bestehende Spalten
  </acceptance_criteria>

  <done>
    User-Verifikation bestaetigt: 5 Status-Uebergaenge (Offen -> Teilbezahlt -> Bezahlt -> Ueberzahlt -> Bezahlt) korrekt sichtbar in der Rechnungs-Liste. Filter funktioniert. Layout sauber. Bestehender Rechnungs-Status weiterhin angezeigt.
  </done>

  <resume-signal>
    Schreibe "approved" wenn alle 5 Status-Uebergaenge korrekt sind und das Layout sauber ist. Bei Problemen: konkrete Beschreibung (z.B. "Badge-Farbe falsch fuer X" oder "Filter funktioniert nicht" oder "Layout bricht bei kleiner Fensterbreite"). Falls statt blau eine andere Farbe fuer 'Ueberzahlt' gewuenscht ist (z.B. rot fuer "Achtung, pruefen") -> dann angeben, wir passen Plan 05-02 an.
  </resume-signal>
</task>

</tasks>

<verification>
**Code-Inspektion:**
```bash
grep -n "function getPaymentStatusBadge" public/js/app.js
grep -n "<th>Zahlung</th>" public/js/app.js
grep -n "getPaymentStatusBadge(inv.payment_status)" public/js/app.js
grep -n "inv-filter-payment-status" public/js/app.js
node --check public/js/app.js
```

**Funktional (Live-Test im Browser):**
- 5 Status-Uebergaenge aus ROADMAP Phase 5 Success Criterion #4 durchgespielt und visuell bestaetigt
- Filter 'Zahlung' funktioniert
- Bestehende Spalte 'Status' und alle anderen Spalten unveraendert
- Keine Browser-Console-Errors
</verification>

<success_criteria>
- getPaymentStatusBadge() existiert in app.js und liefert die 4 Status-Badges mit kapitalisierten Anzeige-Strings
- Tabelle hat 9 Spalten (vorher 8), neue Spalte 'Zahlung' ist zwischen Status und Aktionen
- Filter-Dropdown fuer payment_status funktioniert
- Badges nutzen ausschliesslich existierende CSS-Klassen (gray, orange, green, blue) — kein neues CSS
- Bestehender getInvoiceStatusBadge(inv.status) und alle anderen renderInvoices-Features (Suchfilter, Datums-Filter, Status-Filter, Sortierung, Doppelklick-Navigation, PDF-Button) unveraendert
- User-Verifikation per Checkpoint approved (5 Status-Uebergaenge live durchgespielt)
</success_criteria>

<output>
After completion, create `.planning/phases/05-status-logik-listen/05-02-SUMMARY.md` with:
- Frontmatter (phase, plan, subsystem=ui, tags=[frontend, badge, table, filter], requires=[05-01], provides=[Status-Badge in Liste], affects=[06-detail-ui — kann gleichen Helper wiederverwenden], requirements-completed=[PAY-STAT-03])
- Accomplishments listing
- Decisions: Badge-Farben (gray/orange/green/blue), 'Ueberzahlt' statt rot weil Info-State, additive Spalte (kein Ersatz fuer manuellen Status), Anzeige mit 'Ue' statt 'Ü'
- Description vom End-to-End-Test
- User-Verifikation-Resultat aus Checkpoint
- Self-Check-Block am Ende
</output>
