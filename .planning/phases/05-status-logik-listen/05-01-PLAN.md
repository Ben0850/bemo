---
phase: 05-status-logik-listen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server.js
autonomous: true
requirements:
  - PAY-STAT-01
  - PAY-STAT-02
  - PAY-API-06

must_haves:
  truths:
    - "GET /api/invoices liefert pro Rechnung das numerische Feld payment_saldo"
    - "GET /api/invoices liefert pro Rechnung den string payment_status aus {offen, teilbezahlt, bezahlt, ueberzahlt}"
    - "GET /api/invoices/:id liefert ebenfalls payment_saldo und payment_status (Konsistenz fuer Phase 6)"
    - "Saldo wird korrekt als SUM(in) - SUM(out) berechnet (in EUR mit 2 Nachkommastellen)"
    - "Test-Rechnung 1000 EUR brutto: 0 EUR -> offen, 400 EUR Eingang -> teilbezahlt, 1000 EUR Eingang -> bezahlt, 1100 EUR Eingang -> ueberzahlt, danach 100 EUR Ausgang -> bezahlt"
    - "Floating-Point-Toleranz greift: 999.999999... gegen total_gross=1000 wird als bezahlt erkannt (nicht teilbezahlt)"
    - "Liste mit n Rechnungen wird mit EINER Query (LEFT JOIN + GROUP BY) berechnet — keine n+1-Abfrage"
    - "Bestehender invoices.status (Entwurf/Versendet/Bezahlt/Storniert/Mahnstufe...) bleibt unveraendert im Response"
  artifacts:
    - path: "server.js"
      provides: "derivePaymentStatus(saldo, total_gross) Helper-Funktion"
      contains: "function derivePaymentStatus"
    - path: "server.js"
      provides: "Erweiterter GET /api/invoices Handler mit LEFT JOIN auf invoice_payments und GROUP BY"
      contains: "payment_saldo"
    - path: "server.js"
      provides: "Erweiterter GET /api/invoices/:id Handler mit Saldo-Berechnung"
      contains: "payment_status"
  key_links:
    - from: "GET /api/invoices Handler"
      to: "invoice_payments table"
      via: "LEFT JOIN invoice_payments p ON p.invoice_id = i.id mit GROUP BY i.id"
      pattern: "LEFT JOIN invoice_payments"
    - from: "GET /api/invoices Handler Response"
      to: "derivePaymentStatus()"
      via: "Map ueber Result-Rows mit Aufruf der Helper-Funktion"
      pattern: "derivePaymentStatus\\("
    - from: "derivePaymentStatus()"
      to: "Floating-Point-Toleranz"
      via: "ABS(saldo - total_gross) < 0.005 (oder Math.abs(...) < 0.005)"
      pattern: "0\\.005|Math\\.abs"
---

<objective>
Backend Status-Logik fuer abgeleiteten Rechnungs-Zahlungsstatus implementieren.

Purpose: Der Rechnungs-Zahlungsstatus soll nicht mehr manuell gesetzt werden, sondern systemweit aus dem Saldo der invoice_payments-Tabelle abgeleitet sein — mit einer einzigen effizienten SQL-Query und Floating-Point-toleranten Vergleichen. Das Frontend in Plan 05-02 konsumiert die zwei neuen Felder direkt.

Output: derivePaymentStatus()-Helper + erweiterte GET /api/invoices und GET /api/invoices/:id Handler mit zwei zusaetzlichen Response-Feldern (payment_saldo: number, payment_status: string).
</objective>

<execution_context>
@C:/Users/BenAlbers/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/BenAlbers/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-schema-backend/04-01-SUMMARY.md
@.planning/phases/04-schema-backend/04-02-SUMMARY.md
@server.js
@db.js

<interfaces>
<!-- Bestehende Schema-Felder, die der neue Handler konsumiert. Aus db.js und server.js extrahiert. -->

invoice_payments Tabelle (db.js Zeilen 437-453):
```sql
CREATE TABLE invoice_payments (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  invoice_id INTEGER NOT NULL,
  direction TEXT NOT NULL CHECK (direction IN ('in','out')),
  amount REAL NOT NULL CHECK (amount > 0),
  payment_date TEXT NOT NULL,
  payment_method TEXT DEFAULT '',
  bank_account_id INTEGER,
  reference TEXT DEFAULT '',
  notes TEXT DEFAULT '',
  booked_by TEXT NOT NULL DEFAULT '',
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (invoice_id) REFERENCES invoices(id) ON DELETE CASCADE,
  FOREIGN KEY (bank_account_id) REFERENCES bank_accounts(id)
)
```

invoices Tabelle (relevante Felder fuer Saldo-Vergleich):
```
total_gross REAL  -- Bruttobetrag der Rechnung (Vergleichswert)
status     TEXT   -- bestehender manueller Status (Entwurf/Offen/Bezahlt/Storniert/Mahnstufe...) — BLEIBT UNANGETASTET
```

Bestehender GET /api/invoices Handler (server.js Zeile 1308):
```javascript
app.get('/api/invoices', (req, res) => {
  const { status, search, date_from, date_to } = req.query;
  let sql = `SELECT i.*,
    CASE WHEN c.customer_type IN ('Firmenkunde','Werkstatt') THEN c.company_name ELSE c.last_name || ', ' || c.first_name END as customer_name
    FROM invoices i JOIN customers c ON i.customer_id = c.id WHERE 1=1`;
  // ... filter clauses ...
  sql += ' ORDER BY i.invoice_date DESC, i.id DESC';
  res.json(queryAll(sql, params));
});
```

Bestehender GET /api/invoices/:id Handler (server.js Zeile 1326):
```javascript
app.get('/api/invoices/:id', (req, res) => {
  const invoice = queryOne(`SELECT i.*, c.first_name, c.last_name, c.company_name, ... FROM invoices i JOIN customers c ON i.customer_id = c.id WHERE i.id = ?`, [Number(req.params.id)]);
  if (!invoice) return res.status(404).json({ error: 'Rechnung nicht gefunden' });
  const items = queryAll('SELECT * FROM invoice_items WHERE invoice_id = ? ORDER BY position, id', [Number(req.params.id)]);
  res.json({ ...invoice, items });
});
```

DB-Helper aus server.js (bereits vorhanden):
- `queryAll(sql, params)` — fuehrt SELECT aus, gibt Array zurueck
- `queryOne(sql, params)` — fuehrt SELECT aus, gibt erste Row oder null zurueck
- `execute(sql, params)` — fuehrt INSERT/UPDATE/DELETE aus
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: derivePaymentStatus()-Helper-Funktion mit Floating-Point-Toleranz hinzufuegen</name>

  <files>server.js</files>

  <read_first>
    - server.js Zeilen 1280-1310 (bestehende invoice-Helper-Funktionen wie recalcInvoiceTotals, generateInvoiceNumber) — neue Helper-Funktion soll im gleichen Stil danach platziert werden
    - server.js Zeilen 1448-1593 (INVOICE PAYMENTS-Block aus Plan 04-02) als Referenz fuer Coding-Style
    - .planning/phases/04-schema-backend/04-01-SUMMARY.md (Schema-Output: amount ist REAL > 0, direction ist 'in' oder 'out')
  </read_first>

  <behavior>
    - Test 1: derivePaymentStatus(0, 1000) === 'offen'
    - Test 2: derivePaymentStatus(400, 1000) === 'teilbezahlt'
    - Test 3: derivePaymentStatus(1000, 1000) === 'bezahlt'
    - Test 4: derivePaymentStatus(1100, 1000) === 'ueberzahlt'
    - Test 5 (Toleranz): derivePaymentStatus(999.9999, 1000) === 'bezahlt' (nicht teilbezahlt — Floating-Point-Toleranz 0.005)
    - Test 6 (Toleranz): derivePaymentStatus(1000.0001, 1000) === 'bezahlt' (nicht ueberzahlt — Floating-Point-Toleranz 0.005)
    - Test 7 (Toleranz Untergrenze): derivePaymentStatus(0.001, 1000) === 'offen' (Math.abs(saldo) < 0.005 -> als 0 betrachtet)
    - Test 8 (Negativ-Saldo, falls jemals): derivePaymentStatus(-50, 1000) === 'offen' (negative Salden werden wie 0 behandelt — Sicherheits-Default)
    - Test 9 (total_gross = 0, Edge-Case): derivePaymentStatus(0, 0) === 'bezahlt' (Saldo gleich Brutto, beide null)
  </behavior>

  <action>
    NEUE Helper-Funktion in server.js direkt NACH `function generateInvoiceNumber() { ... }` einfuegen (zwischen den bestehenden Invoice-Helpern und dem ersten `app.get('/api/invoices'...)` Handler — also vor Zeile 1308).

    Genauer Code (genau so einfuegen):
    ```javascript
    // Phase 5 (PAY-STAT-01, PAY-STAT-02): Zahlungsstatus aus Saldo ableiten.
    // saldo = SUM(direction='in') - SUM(direction='out') aus invoice_payments
    // Vergleich gegen invoices.total_gross mit Floating-Point-Toleranz von 0.005 EUR
    // (haelfte des kleinsten Cent-Wertes — verhindert dass 999.999999... vs 1000.00 als teilbezahlt gilt)
    function derivePaymentStatus(saldo, total_gross) {
      const s = Number(saldo) || 0;
      const g = Number(total_gross) || 0;
      const TOL = 0.005;
      // saldo praktisch 0 oder negativ -> offen
      if (s < TOL) return 'offen';
      // saldo praktisch gleich brutto -> bezahlt
      if (Math.abs(s - g) < TOL) return 'bezahlt';
      // saldo deutlich groesser als brutto -> ueberzahlt
      if (s > g) return 'ueberzahlt';
      // saldo deutlich kleiner als brutto -> teilbezahlt
      return 'teilbezahlt';
    }
    ```

    WICHTIG: Status-Werte sind kleingeschrieben ohne Umlaute: 'offen', 'teilbezahlt', 'bezahlt', 'ueberzahlt'. Das Frontend mappt sie spaeter auf Anzeige-Strings.

    Konsistenz-Hinweis: REQUIREMENTS.md Zeile 29 schreibt "Offen/Teilbezahlt/Bezahlt/Ueberzahlt" mit Grossbuchstaben — wir liefern die Backend-Werte aber lowercase und ohne Umlaut (technisch sauber, frontend-stabil), und das Frontend macht das Capitalize+Umlaut-Mapping in Plan 05-02. Diese Entscheidung wird in der SUMMARY dokumentiert.
  </action>

  <verify>
    <automated>
      grep -n "function derivePaymentStatus" server.js \
        && grep -n "'offen'" server.js \
        && grep -n "'teilbezahlt'" server.js \
        && grep -n "'bezahlt'" server.js \
        && grep -n "'ueberzahlt'" server.js \
        && grep -n "0\.005\|TOL" server.js \
        && node --check server.js \
        && node -e "const {execSync}=require('child_process');" \
              -e "const code = require('fs').readFileSync('server.js','utf8');" \
              -e "const fnMatch = code.match(/function derivePaymentStatus\\([\\s\\S]*?\\n\\}/);" \
              -e "if(!fnMatch) { console.error('FAIL: function not found'); process.exit(1); }" \
              -e "eval(fnMatch[0]);" \
              -e "const tests = [[0,1000,'offen'],[400,1000,'teilbezahlt'],[1000,1000,'bezahlt'],[1100,1000,'ueberzahlt'],[999.9999,1000,'bezahlt'],[1000.0001,1000,'bezahlt'],[0.001,1000,'offen'],[-50,1000,'offen'],[0,0,'bezahlt']];" \
              -e "for (const [s,g,exp] of tests) { const got = derivePaymentStatus(s,g); if(got!==exp){console.error('FAIL',s,g,'->',got,'expected',exp); process.exit(1);} }" \
              -e "console.log('OK 9/9');"
    </automated>
  </verify>

  <acceptance_criteria>
    - server.js enthaelt exakten String "function derivePaymentStatus"
    - server.js enthaelt alle 4 Status-Strings: 'offen', 'teilbezahlt', 'bezahlt', 'ueberzahlt' (lowercase, ohne Umlaut)
    - server.js enthaelt Floating-Point-Toleranz 0.005 (oder konstante TOL = 0.005)
    - `node --check server.js` -> exit 0 (kein Syntaxfehler)
    - Alle 9 Behavior-Tests aus dem Inline-Node-Eval gruen
    - Funktion ist VOR `app.get('/api/invoices', ...)` Handler definiert (Zeile 1308) — sonst ReferenceError beim ersten Request
  </acceptance_criteria>

  <done>
    derivePaymentStatus(saldo, total_gross) ist eine reine Funktion in server.js, gibt einen der 4 Status-Strings zurueck, beruecksichtigt Floating-Point-Toleranz von 0.005 EUR und ist von den nachfolgenden Endpoint-Handlern (Task 2 + Task 3) aufrufbar.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GET /api/invoices um payment_saldo + payment_status erweitern (LEFT JOIN + GROUP BY, eine Query)</name>

  <files>server.js</files>

  <read_first>
    - server.js Zeilen 1308-1324 (bestehender GET /api/invoices Handler — wird erweitert, NICHT ersetzt)
    - server.js Zeilen 1326-1332 (GET /api/invoices/:id als Style-Referenz fuer JOIN-Aufbau)
    - .planning/phases/04-schema-backend/04-01-SUMMARY.md (idx_invoice_payments_invoice_date Index ist bereits angelegt — JOIN ist performant)
  </read_first>

  <behavior>
    - Test 1: GET /api/invoices ohne Filter -> jede Rechnung im Response hat Felder payment_saldo (number) und payment_status (string)
    - Test 2: Rechnung ohne irgendeine invoice_payments-Row -> payment_saldo === 0, payment_status === "offen"
    - Test 3: Rechnung mit nur Eingaengen 400 EUR -> payment_saldo === 400, payment_status === "teilbezahlt" (bei total_gross=1000)
    - Test 4: Rechnung mit Eingang 500 EUR und Ausgang 100 EUR -> payment_saldo === 400, payment_status === "teilbezahlt"
    - Test 5: Rechnung mit Eingang 1000 EUR (=total_gross) -> payment_status === "bezahlt"
    - Test 6: Rechnung mit Eingang 1100 EUR -> payment_status === "ueberzahlt"
    - Test 7: Bestehender Filter `?status=Entwurf` filtert weiterhin nach manuellem invoices.status — UNVERAENDERTES Verhalten
    - Test 8: Bestehende Felder (invoice_number, customer_name, total_gross, ...) bleiben in jedem Response-Objekt vorhanden
    - Test 9 (Performance/n+1-Garantie): Code enthaelt nur EINE queryAll-Query in diesem Handler (kein zusaetzlicher Loop mit DB-Aufrufen)
  </behavior>

  <action>
    Bestehenden Handler `app.get('/api/invoices', (req, res) => { ... })` an Zeile 1308 ERSETZEN durch erweiterten Handler.

    Genauer neuer Code (ersetzt Zeilen 1308-1324):
    ```javascript
    app.get('/api/invoices', (req, res) => {
      const { status, search, date_from, date_to } = req.query;
      // Phase 5 (PAY-API-06): Eine Query mit LEFT JOIN + GROUP BY berechnet payment_saldo
      // pro Rechnung — verhindert n+1-Abfragen bei langen Rechnungslisten.
      // Die invoice_payments-Tabelle hat den Index idx_invoice_payments_invoice_date,
      // sodass der JOIN auch bei vielen tausend Zahlungen performant bleibt.
      let sql = `SELECT i.*,
        CASE WHEN c.customer_type IN ('Firmenkunde','Werkstatt') THEN c.company_name ELSE c.last_name || ', ' || c.first_name END as customer_name,
        COALESCE(SUM(CASE WHEN p.direction='in'  THEN p.amount ELSE 0 END), 0)
        - COALESCE(SUM(CASE WHEN p.direction='out' THEN p.amount ELSE 0 END), 0) AS payment_saldo
        FROM invoices i
        JOIN customers c ON i.customer_id = c.id
        LEFT JOIN invoice_payments p ON p.invoice_id = i.id
        WHERE 1=1`;
      const params = [];
      if (status)    { sql += ' AND i.status = ?'; params.push(status); }
      if (date_from) { sql += ' AND i.invoice_date >= ?'; params.push(date_from); }
      if (date_to)   { sql += ' AND i.invoice_date <= ?'; params.push(date_to); }
      if (search) {
        const term = `%${search}%`;
        sql += ' AND (i.invoice_number LIKE ? OR c.last_name LIKE ? OR c.first_name LIKE ? OR c.company_name LIKE ?)';
        params.push(term, term, term, term);
      }
      sql += ' GROUP BY i.id ORDER BY i.invoice_date DESC, i.id DESC';
      const rows = queryAll(sql, params);
      // Phase 5 (PAY-STAT-02): payment_status pro Row in JS ableiten
      // (Status-Logik in JS statt SQL macht Refactoring der Toleranzgrenzen einfach)
      const result = rows.map(r => ({
        ...r,
        payment_saldo: Math.round((Number(r.payment_saldo) || 0) * 100) / 100, // 2 Nachkommastellen
        payment_status: derivePaymentStatus(r.payment_saldo, r.total_gross)
      }));
      res.json(result);
    });
    ```

    WICHTIG:
    - GROUP BY i.id ist Pflicht wegen SUM(CASE...) — sonst kollabiert die ganze Liste auf eine Row.
    - Bei SQLite: `SELECT i.*` zusammen mit GROUP BY i.id ist erlaubt (kein strict-mode wie MySQL ONLY_FULL_GROUP_BY).
    - LEFT JOIN ist Pflicht: Rechnungen ohne Zahlungen sollen mit payment_saldo=0 erscheinen (INNER JOIN wuerde sie ausblenden).
    - Math.round(x * 100) / 100 verhindert dass Floating-Point-Artefakte (z.B. 399.99999999) im Frontend ankommen.
    - Der bestehende invoices.status (manuell gesetzt: Entwurf/Offen/Bezahlt/Storniert/Mahnstufe...) bleibt im Response unter `status` UNVERAENDERT — payment_status ist ein additives Feld.
  </action>

  <verify>
    <automated>
      # Statisch: Code-Inspektion
      grep -n "LEFT JOIN invoice_payments" server.js \
        && grep -n "GROUP BY i\\.id" server.js \
        && grep -n "payment_saldo" server.js \
        && grep -n "derivePaymentStatus(r\\.payment_saldo" server.js \
        && grep -n "Math\\.round" server.js \
        && node --check server.js

      # Dynamisch: Server starten und live verifizieren
      # Voraussetzung: PowerShell-/Bash-Zugang, server.js auf Port 3001
      # 1. Alten Server killen falls aktiv: taskkill -f -im node.exe || true
      # 2. node server.js & sleep 2
      # 3. Headers sind erforderlich (x-user-permission: Verwaltung, x-user-name: planner-test)
      H='-H "x-user-permission: Verwaltung" -H "x-user-name: planner-test" -H "Content-Type: application/json"'

      # Test-Rechnung anlegen (erfordert Customer ID — bestehend nutzen, z.B. customer_id=1)
      INV=$(curl -s -X POST http://localhost:3001/api/invoices -H "x-user-permission: Verwaltung" -H "x-user-name: planner-test" -H "Content-Type: application/json" -d '{"customer_id":1,"invoice_date":"2026-04-28","due_date":"2026-05-12","service_date":"2026-04-28","payment_method":"Ueberweisung","notes":"Phase 5 Test"}')
      INV_ID=$(echo "$INV" | node -e "let d=''; process.stdin.on('data',c=>d+=c); process.stdin.on('end',()=>{console.log(JSON.parse(d).id);})")

      # Position 1000 EUR brutto hinzufuegen (vat_rate 0 -> total_gross = total_net = qty*unit_price)
      curl -s -X POST http://localhost:3001/api/invoices/$INV_ID/items -H "x-user-permission: Verwaltung" -H "x-user-name: planner-test" -H "Content-Type: application/json" -d '{"description":"Test","quantity":1,"unit_price":1000,"vat_rate":0}' > /dev/null

      # GET /api/invoices Liste pruefen
      LIST=$(curl -s http://localhost:3001/api/invoices)
      echo "$LIST" | node -e "let d=''; process.stdin.on('data',c=>d+=c); process.stdin.on('end',()=>{const arr=JSON.parse(d); const r=arr.find(x=>x.id===$INV_ID); if(!r){console.error('FAIL: invoice not in list'); process.exit(1);} if(typeof r.payment_saldo!=='number'){console.error('FAIL: payment_saldo not number',r.payment_saldo); process.exit(1);} if(r.payment_saldo!==0){console.error('FAIL: empty saldo expected 0, got',r.payment_saldo); process.exit(1);} if(r.payment_status!=='offen'){console.error('FAIL: empty status expected offen, got',r.payment_status); process.exit(1);} console.log('OK leer: saldo=0 status=offen');})"

      # Eingang 400 EUR
      curl -s -X POST http://localhost:3001/api/invoices/$INV_ID/payments -H "x-user-permission: Verwaltung" -H "x-user-name: planner-test" -H "Content-Type: application/json" -d '{"direction":"in","amount":400,"payment_date":"2026-04-28","payment_method":"Ueberweisung"}' > /dev/null
      curl -s http://localhost:3001/api/invoices | node -e "let d=''; process.stdin.on('data',c=>d+=c); process.stdin.on('end',()=>{const r=JSON.parse(d).find(x=>x.id===$INV_ID); if(r.payment_saldo!==400 || r.payment_status!=='teilbezahlt'){console.error('FAIL 400 in:',r.payment_saldo,r.payment_status); process.exit(1);} console.log('OK 400 in -> 400/teilbezahlt');})"

      # Eingang 600 EUR -> Saldo 1000 -> bezahlt
      curl -s -X POST http://localhost:3001/api/invoices/$INV_ID/payments -H "x-user-permission: Verwaltung" -H "x-user-name: planner-test" -H "Content-Type: application/json" -d '{"direction":"in","amount":600,"payment_date":"2026-04-28","payment_method":"Ueberweisung"}' > /dev/null
      curl -s http://localhost:3001/api/invoices | node -e "let d=''; process.stdin.on('data',c=>d+=c); process.stdin.on('end',()=>{const r=JSON.parse(d).find(x=>x.id===$INV_ID); if(r.payment_saldo!==1000 || r.payment_status!=='bezahlt'){console.error('FAIL +600:',r.payment_saldo,r.payment_status); process.exit(1);} console.log('OK +600 -> 1000/bezahlt');})"

      # Eingang 100 EUR -> Saldo 1100 -> ueberzahlt
      curl -s -X POST http://localhost:3001/api/invoices/$INV_ID/payments -H "x-user-permission: Verwaltung" -H "x-user-name: planner-test" -H "Content-Type: application/json" -d '{"direction":"in","amount":100,"payment_date":"2026-04-28","payment_method":"Ueberweisung"}' > /dev/null
      curl -s http://localhost:3001/api/invoices | node -e "let d=''; process.stdin.on('data',c=>d+=c); process.stdin.on('end',()=>{const r=JSON.parse(d).find(x=>x.id===$INV_ID); if(r.payment_saldo!==1100 || r.payment_status!=='ueberzahlt'){console.error('FAIL +100:',r.payment_saldo,r.payment_status); process.exit(1);} console.log('OK +100 -> 1100/ueberzahlt');})"

      # Ausgang 100 EUR -> Saldo 1000 -> bezahlt
      curl -s -X POST http://localhost:3001/api/invoices/$INV_ID/payments -H "x-user-permission: Verwaltung" -H "x-user-name: planner-test" -H "Content-Type: application/json" -d '{"direction":"out","amount":100,"payment_date":"2026-04-28","payment_method":"Ueberweisung"}' > /dev/null
      curl -s http://localhost:3001/api/invoices | node -e "let d=''; process.stdin.on('data',c=>d+=c); process.stdin.on('end',()=>{const r=JSON.parse(d).find(x=>x.id===$INV_ID); if(r.payment_saldo!==1000 || r.payment_status!=='bezahlt'){console.error('FAIL out 100:',r.payment_saldo,r.payment_status); process.exit(1);} console.log('OK out 100 -> 1000/bezahlt');})"

      # Cleanup: Test-Rechnung loeschen (Cascade entfernt invoice_payments)
      # invoice.status muss "Entwurf" sein damit DELETE durchgeht
      curl -s -X DELETE http://localhost:3001/api/invoices/$INV_ID -H "x-user-permission: Admin" -H "x-user-name: planner-test" > /dev/null
      echo "ALL 5 SCENARIOS PASS"
    </automated>
  </verify>

  <acceptance_criteria>
    - server.js enthaelt exakten String "LEFT JOIN invoice_payments"
    - server.js enthaelt exakten String "GROUP BY i.id" im /api/invoices Handler
    - server.js enthaelt zwei COALESCE(SUM(CASE WHEN p.direction='in'/'out')) Klauseln
    - server.js enthaelt `derivePaymentStatus(r.payment_saldo, r.total_gross)` Aufruf
    - server.js enthaelt Math.round-Rundung des payment_saldo auf 2 Nachkommastellen
    - `node --check server.js` -> exit 0
    - Live-curl (laufender Server) liefert auf GET /api/invoices ein Array, in dem jedes Element ein numerisches `payment_saldo` und einen string `payment_status` aus {offen, teilbezahlt, bezahlt, ueberzahlt} hat
    - End-to-End-Szenario aus REQUIREMENTS.md/ROADMAP.md vollstaendig durchlaufen: 0 -> offen, +400 -> teilbezahlt, +600 -> bezahlt, +100 -> ueberzahlt, -100 -> bezahlt
    - Bestehender Filter `?status=Entwurf` funktioniert weiterhin (manueller invoices.status unangetastet)
    - Code enthaelt KEINE n+1-Schleife (nur EIN queryAll im Handler — pruefbar via grep)
  </acceptance_criteria>

  <done>
    GET /api/invoices liefert pro Rechnung zwei zusaetzliche Felder (payment_saldo, payment_status), berechnet in einer einzigen Query mit LEFT JOIN + GROUP BY, status-abgeleitet via derivePaymentStatus(). Alle 5 End-to-End-Szenarien der Phase-5-Success-Criteria durchlaufen.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GET /api/invoices/:id ebenfalls um payment_saldo + payment_status erweitern (Konsistenz fuer Phase 6)</name>

  <files>server.js</files>

  <read_first>
    - server.js Zeilen 1326-1332 (bestehender GET /api/invoices/:id Handler) — wird erweitert, items-Loading bleibt erhalten
    - server.js Zeilen 1448-1465 (GET /api/invoices/:id/payments aus Plan 04-02) als Referenz fuer payments-Query-Stil
  </read_first>

  <behavior>
    - Test 1: GET /api/invoices/:id existierende Rechnung -> Response enthaelt payment_saldo (number) und payment_status (string) ZUSAETZLICH zu allen bestehenden Feldern (id, invoice_number, customer_name, total_gross, items, ...)
    - Test 2: Rechnung ohne Zahlungen -> payment_saldo === 0, payment_status === "offen"
    - Test 3: Rechnung mit Eingang 500 EUR (bei total_gross=1000) -> payment_saldo === 500, payment_status === "teilbezahlt"
    - Test 4: GET /api/invoices/99999 (unbekannt) -> liefert weiterhin 404 mit {error:'Rechnung nicht gefunden'} — UNVERAENDERTES Verhalten
    - Test 5: items[]-Array bleibt im Response erhalten (bestehender Loader fuer invoice_items unveraendert)
  </behavior>

  <action>
    Bestehenden Handler `app.get('/api/invoices/:id', (req, res) => { ... })` an Zeile 1326 ERSETZEN durch erweiterten Handler.

    Genauer neuer Code (ersetzt Zeilen 1326-1332):
    ```javascript
    app.get('/api/invoices/:id', (req, res) => {
      const id = Number(req.params.id);
      const invoice = queryOne(`SELECT i.*, c.first_name, c.last_name, c.company_name, c.customer_type, c.street, c.zip, c.city, c.email, c.phone
        FROM invoices i JOIN customers c ON i.customer_id = c.id WHERE i.id = ?`, [id]);
      if (!invoice) return res.status(404).json({ error: 'Rechnung nicht gefunden' });
      const items = queryAll('SELECT * FROM invoice_items WHERE invoice_id = ? ORDER BY position, id', [id]);
      // Phase 5 (PAY-API-06 / PAY-STAT-01/02): payment_saldo + payment_status auch im Detail-Endpoint,
      // konsistent mit GET /api/invoices. Nutzt eigene aggregate Query (kein zusaetzlicher Loop).
      const saldoRow = queryOne(
        `SELECT COALESCE(SUM(CASE WHEN direction='in'  THEN amount ELSE 0 END), 0)
              - COALESCE(SUM(CASE WHEN direction='out' THEN amount ELSE 0 END), 0) AS saldo
         FROM invoice_payments WHERE invoice_id = ?`,
        [id]
      );
      const payment_saldo = Math.round((Number(saldoRow?.saldo) || 0) * 100) / 100;
      const payment_status = derivePaymentStatus(payment_saldo, invoice.total_gross);
      res.json({ ...invoice, items, payment_saldo, payment_status });
    });
    ```

    WICHTIG:
    - 404-Pfad UNVERAENDERT — Frontend in Phase 6 verlaesst sich auf den 404-Status.
    - items[]-Array bleibt vorhanden (bestehende Detailseite rendert es).
    - Math.round wieder fuer 2 Nachkommastellen — gleiche Konvention wie in Task 2.
    - `saldoRow?.saldo` — `?.` schuetzt vor null bei Aggregat-Edge-Cases (sollte nie eintreten, defensive Praxis).
    - Reihenfolge im Response-Spread: `{...invoice, items, payment_saldo, payment_status}` — items zuerst (bestehende Konvention), dann die zwei neuen Felder.
  </action>

  <verify>
    <automated>
      # Statisch
      grep -n "GET.*invoices.*:id" server.js | head -5
      grep -n "payment_saldo, payment_status" server.js \
        && grep -n "FROM invoice_payments WHERE invoice_id = ?" server.js \
        && node --check server.js

      # Dynamisch
      H_VW='-H "x-user-permission: Verwaltung" -H "x-user-name: planner-test" -H "Content-Type: application/json"'
      INV=$(curl -s -X POST http://localhost:3001/api/invoices -H "x-user-permission: Verwaltung" -H "x-user-name: planner-test" -H "Content-Type: application/json" -d '{"customer_id":1,"invoice_date":"2026-04-28","due_date":"2026-05-12","service_date":"2026-04-28","payment_method":"Ueberweisung","notes":"Phase 5 Detail-Test"}')
      INV_ID=$(echo "$INV" | node -e "let d=''; process.stdin.on('data',c=>d+=c); process.stdin.on('end',()=>{console.log(JSON.parse(d).id);})")
      curl -s -X POST http://localhost:3001/api/invoices/$INV_ID/items -H "x-user-permission: Verwaltung" -H "x-user-name: planner-test" -H "Content-Type: application/json" -d '{"description":"Test","quantity":1,"unit_price":1000,"vat_rate":0}' > /dev/null

      # Test 1+2: Detail leer
      curl -s http://localhost:3001/api/invoices/$INV_ID | node -e "let d=''; process.stdin.on('data',c=>d+=c); process.stdin.on('end',()=>{const r=JSON.parse(d); if(typeof r.payment_saldo!=='number'){console.error('FAIL: not number',r.payment_saldo); process.exit(1);} if(r.payment_saldo!==0||r.payment_status!=='offen'){console.error('FAIL leer:',r.payment_saldo,r.payment_status); process.exit(1);} if(!Array.isArray(r.items)){console.error('FAIL: items missing'); process.exit(1);} console.log('OK detail leer');})"

      # Test 3: Eingang 500 -> teilbezahlt
      curl -s -X POST http://localhost:3001/api/invoices/$INV_ID/payments -H "x-user-permission: Verwaltung" -H "x-user-name: planner-test" -H "Content-Type: application/json" -d '{"direction":"in","amount":500,"payment_date":"2026-04-28","payment_method":"Ueberweisung"}' > /dev/null
      curl -s http://localhost:3001/api/invoices/$INV_ID | node -e "let d=''; process.stdin.on('data',c=>d+=c); process.stdin.on('end',()=>{const r=JSON.parse(d); if(r.payment_saldo!==500||r.payment_status!=='teilbezahlt'){console.error('FAIL 500:',r.payment_saldo,r.payment_status); process.exit(1);} console.log('OK detail 500');})"

      # Test 4: 404
      curl -s -o /dev/null -w "%{http_code}" http://localhost:3001/api/invoices/99999 | grep -q "404" && echo "OK 404" || (echo "FAIL 404"; exit 1)

      # Cleanup
      curl -s -X DELETE http://localhost:3001/api/invoices/$INV_ID -H "x-user-permission: Admin" -H "x-user-name: planner-test" > /dev/null
      echo "ALL DETAIL TESTS PASS"
    </automated>
  </verify>

  <acceptance_criteria>
    - server.js enthaelt im /api/invoices/:id Handler den String "FROM invoice_payments WHERE invoice_id = ?"
    - server.js enthaelt im Response-Spread "payment_saldo, payment_status"
    - server.js enthaelt weiterhin den 404-Pfad `return res.status(404).json({ error: 'Rechnung nicht gefunden' })`
    - server.js enthaelt weiterhin das items-Array-Loading via queryAll('SELECT * FROM invoice_items ...')
    - `node --check server.js` -> exit 0
    - Live-curl auf GET /api/invoices/:id (existierende Rechnung) liefert payment_saldo (number) und payment_status (string) zusaetzlich zu items
    - Live-curl auf GET /api/invoices/99999 liefert weiterhin HTTP 404
    - Test 3 (500 EUR eingang -> teilbezahlt) durchlaufen
  </acceptance_criteria>

  <done>
    GET /api/invoices/:id liefert konsistent zur Liste die Felder payment_saldo und payment_status. items-Array und 404-Verhalten sind unangetastet. Phase 6 (Detail-UI) kann diese Felder ohne weitere Backend-Aenderungen konsumieren.
  </done>
</task>

</tasks>

<verification>
**Code-Inspektion:**
```bash
grep -n "function derivePaymentStatus" server.js   # Helper exists
grep -n "LEFT JOIN invoice_payments" server.js      # Liste-Handler hat JOIN
grep -n "GROUP BY i\.id" server.js                  # GROUP BY vorhanden
grep -c "payment_saldo" server.js                   # >=4 (Helper-Aufrufe + Response-Felder)
grep -c "payment_status" server.js                  # >=4
node --check server.js                              # Syntax OK
```

**End-to-End (laufender Server):**
1. Test-Rechnung anlegen (1000 EUR brutto, customer_id=1)
2. `GET /api/invoices` Liste -> Rechnung hat payment_saldo=0, payment_status="offen"
3. `GET /api/invoices/:id` Detail -> gleiche Felder + items[]
4. POST Zahlung 400 EUR ein -> Liste zeigt payment_status="teilbezahlt"
5. POST Zahlung 600 EUR ein (Summe 1000) -> Liste zeigt payment_status="bezahlt"
6. POST Zahlung 100 EUR ein (Summe 1100) -> Liste zeigt payment_status="ueberzahlt"
7. POST Zahlung 100 EUR aus (Summe 1000) -> Liste zeigt payment_status="bezahlt"
8. Cleanup via DELETE /api/invoices/:id (Cascade entfernt payments)

**Bestehende Endpoints unangetastet:**
- POST /api/invoices, PUT /api/invoices/:id, DELETE /api/invoices/:id unveraendert
- /api/invoices/:id/payments (Plan 04-02) unveraendert
- invoices.status (manueller Status) bleibt im Response unter `status` — wird vom Frontend in Plan 05-02 weiterhin als Alt-Badge gezeigt oder bewusst ersetzt (Plan-05-02-Entscheidung)
</verification>

<success_criteria>
- derivePaymentStatus() existiert in server.js und liefert die 4 Status-Strings korrekt mit Floating-Point-Toleranz 0.005
- GET /api/invoices liefert pro Rechnung payment_saldo (number, 2 Nachkommastellen) und payment_status (string)
- GET /api/invoices/:id liefert beide Felder ebenfalls
- Berechnung erfolgt mit EINER Query (LEFT JOIN + GROUP BY) — keine n+1
- End-to-End-Szenario aus ROADMAP.md Phase 5 Success Criteria #4 vollstaendig durchlaufen (1000 EUR Brutto -> 5 Status-Wechsel)
- Bestehender invoices.status unveraendert
- node --check server.js exit 0, kein Syntaxfehler
</success_criteria>

<output>
After completion, create `.planning/phases/05-status-logik-listen/05-01-SUMMARY.md` with:
- Frontmatter (phase, plan, subsystem=api, tags, requires=[Plan 04-01,04-02], provides, affects=[05-02], requirements-completed=[PAY-STAT-01, PAY-STAT-02, PAY-API-06])
- Accomplishments listing
- Decision: lowercase Status-Strings ohne Umlaut (technisch sauber, Frontend macht Display-Mapping)
- Decision: Floating-Point-Toleranz 0.005 EUR (=halber Cent)
- E2E-Verifikations-Tabelle
- Self-Check-Block am Ende
</output>
