---
phase: 04-schema-backend
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - server.js
autonomous: true
requirements:
  - PAY-API-01
  - PAY-API-02
  - PAY-API-03
  - PAY-API-04
  - PAY-API-05

must_haves:
  truths:
    - "GET /api/invoices/:id/payments liefert ein nach payment_date ASC sortiertes Array aller Zahlungen einer Rechnung (200 + JSON-Array, leeres Array bei Rechnung ohne Zahlungen)"
    - "POST /api/invoices/:id/payments mit Body {direction, amount, payment_date} und Header x-user-permission: Verwaltung liefert HTTP 201 mit dem angelegten Datensatz inkl. id und booked_by"
    - "POST /api/invoices/:id/payments setzt booked_by automatisch aus req.headers['x-user-name'] (NICHT aus dem Body)"
    - "PUT /api/payments/:id mit Header x-user-permission: Buchhaltung aktualisiert alle Felder außer booked_by und created_at — beide bleiben unverändert"
    - "DELETE /api/payments/:id mit Header x-user-permission: Admin löscht den Datensatz (HTTP 200/204)"
    - "POST/PUT/DELETE mit x-user-permission: Benutzer liefern HTTP 403 mit error: 'Keine Berechtigung'"
    - "POST/PUT/DELETE OHNE x-user-permission Header (oder unbekannte Rolle) liefern HTTP 403"
    - "GET-Endpoint hat KEINEN Permission-Guard (Lesezugriff für alle eingeloggten Rollen)"
    - "POST mit invalidem Body (direction='other' oder amount=0) liefert HTTP 400 oder 500 mit klarer Fehlermeldung (CHECK-Constraint-Verletzung wird aufgefangen)"
  artifacts:
    - path: "server.js"
      provides: "Vier CRUD-Endpoints für invoice_payments mit Permission-Guards"
      contains: "app.get('/api/invoices/:id/payments'"
  key_links:
    - from: "server.js POST /api/invoices/:id/payments"
      to: "invoice_payments table"
      via: "execute() INSERT mit booked_by aus x-user-name Header"
      pattern: "INSERT INTO invoice_payments"
    - from: "server.js POST /api/invoices/:id/payments"
      to: "Permission-Guard"
      via: "x-user-permission Header-Check gegen ['Verwaltung','Buchhaltung','Admin']"
      pattern: "\\['Verwaltung', 'Buchhaltung', 'Admin'\\]"
    - from: "server.js PUT /api/payments/:id"
      to: "invoice_payments table"
      via: "execute() UPDATE — schließt booked_by und created_at explizit aus"
      pattern: "UPDATE invoice_payments SET"
    - from: "server.js DELETE /api/payments/:id"
      to: "invoice_payments table"
      via: "execute() DELETE mit Permission-Guard (analog zu /api/invoices)"
      pattern: "DELETE FROM invoice_payments WHERE id"
---

<objective>
Implementiere vier HTTP-Endpoints für invoice_payments-CRUD mit Permission-Guards: GET (Liste), POST (anlegen), PUT (ändern), DELETE (löschen). Alle Schreib-Endpoints sind auf Verwaltung/Buchhaltung/Admin beschränkt — analog zu /api/invoices. POST setzt booked_by automatisch aus dem x-user-name Header.

Purpose: Macht die Tabelle aus Plan 04-01 nutzbar. Ist das Backend-Deliverable für die Detail-UI in Phase 6, und stellt die Datengrundlage für die Saldo-Berechnung in Phase 5 bereit.
Output: Aktualisierte server.js mit 4 neuen Endpoints (eingefügt nach den bestehenden /api/invoices/:id/items-Routen, ~Zeile 1442).
</objective>

<execution_context>
@C:/Users/BenAlbers/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/BenAlbers/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-schema-backend/04-01-PLAN.md

<interfaces>
<!-- Permission-Guard-Pattern — exakt aus /api/invoices POST (server.js Zeile 1330-1335) -->
From server.js (Zeile 1330-1335):
```javascript
app.post('/api/invoices', (req, res) => {
  // AUTH-02: Nur Verwaltung, Buchhaltung, Admin dürfen Rechnungen erstellen
  const permission = req.headers['x-user-permission'];
  if (!['Verwaltung', 'Buchhaltung', 'Admin'].includes(permission)) {
    return res.status(403).json({ error: 'Keine Berechtigung' });
  }
```

<!-- PUT-Pattern aus /api/invoices PUT (server.js Zeile 1359-1373) -->
From server.js (Zeile 1359-1373):
```javascript
app.put('/api/invoices/:id', (req, res) => {
  const permission = req.headers['x-user-permission'];
  if (!['Verwaltung', 'Buchhaltung', 'Admin'].includes(permission)) {
    return res.status(403).json({ error: 'Keine Berechtigung' });
  }
  const { due_date, status, service_date, payment_method, notes } = req.body;
  execute(
    'UPDATE invoices SET due_date=?, status=?, ... updated_at=CURRENT_TIMESTAMP WHERE id=?',
    [due_date || '', status || 'Entwurf', ..., Number(req.params.id)]
  );
  res.json({ message: 'Rechnung aktualisiert' });
});
```

<!-- DELETE-Pattern (mit globalem Admin-Middleware) — aus /api/invoices DELETE (server.js Zeile 1375-1385) -->
From server.js (Zeile 53-62) — globales Middleware:
```javascript
app.use((req, res, next) => {
  if (req.method === 'DELETE') {
    const permission = req.headers['x-user-permission'];
    if (permission !== 'Admin' && !req.path.startsWith('/api/calendar/') && !req.path.startsWith('/api/time/') && !req.path.startsWith('/api/files/')) {
      return res.status(403).json({ error: 'Nur Admins dürfen Einträge löschen' });
    }
  }
  next();
});
```
WICHTIG: Das globale Middleware beschränkt DELETE auf Admin. Für Verwaltung/Buchhaltung als zusätzliche erlaubte Rollen muss der Inline-Guard im DELETE-Handler die Logik überschreiben — aber das globale Middleware blockiert das BEVOR der Handler läuft. **Lösung wie in Plan 01-02 v1.0:** Der globale Middleware lässt nur Admin durch. Wenn die Anforderung also "Verwaltung + Buchhaltung + Admin dürfen löschen" lautet, müssen wir den Pfad `/api/payments/` zur Whitelist im globalen Middleware hinzufügen UND einen Inline-Guard im DELETE-Handler ergänzen, der die 3 erlaubten Rollen prüft.

<!-- x-user-name Header-Pattern (server.js Zeile 3037) -->
From server.js (Zeile 3037):
```javascript
const username = req.headers['x-user-name'] || '';
```

<!-- DB-Helpers — aus db.js -->
```javascript
const { getDb, queryAll, queryOne, execute } = require('./db');
// queryAll(sql, params) -> array of row objects
// queryOne(sql, params) -> first row or null
// execute(sql, params) -> { lastId } (auto-saves)
```

<!-- bank_accounts-Tabelle existiert (db.js Zeile 422-432) — bank_account_id darf NULL sein -->
<!-- invoices-Tabelle (db.js Zeile 150-165) — invoice_id muss auf existierende Rechnung zeigen -->

<!-- Neue Tabelle aus Plan 04-01 — ihre Spalten -->
invoice_payments columns (aus Plan 04-01):
- id (auto-increment)
- invoice_id (NOT NULL, FK invoices ON DELETE CASCADE)
- direction (NOT NULL, CHECK IN ('in','out'))
- amount (NOT NULL, CHECK > 0)
- payment_date (NOT NULL)
- payment_method (DEFAULT '')
- bank_account_id (nullable, FK bank_accounts)
- reference (DEFAULT '')
- notes (DEFAULT '')
- booked_by (NOT NULL, DEFAULT '')
- created_at (DEFAULT CURRENT_TIMESTAMP)
- updated_at (DEFAULT CURRENT_TIMESTAMP)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GET-Endpoint und globales DELETE-Middleware Whitelist anpassen</name>
  <files>server.js</files>
  <read_first>
    - C:/Claude/Bemo_v2/server.js Zeilen 53-62 (globales DELETE-Middleware — `/api/payments/` muss in die Whitelist, da Verwaltung/Buchhaltung dort löschen dürfen)
    - C:/Claude/Bemo_v2/server.js Zeilen 1304-1442 (bestehende /api/invoices Endpoints — Stilvorlage)
    - C:/Claude/Bemo_v2/server.js Zeilen 1437-1442 (DELETE /api/invoice-items — direkt davor/danach werden die neuen Endpoints eingefügt)
    - C:/Claude/Bemo_v2/.planning/phases/04-schema-backend/04-01-PLAN.md (Spaltenliste der neuen Tabelle)
  </read_first>
  <action>
**Schritt A: Globales DELETE-Middleware (Zeile 53-62) erweitern.**

Das aktuelle Middleware blockiert ALLE DELETE-Anfragen, die NICHT von Admin kommen — mit Ausnahme von `/api/calendar/`, `/api/time/`, `/api/files/`. Da Plan 04-02 erlaubt, dass auch Verwaltung und Buchhaltung Zahlungen löschen, muss `/api/payments/` zur Whitelist hinzugefügt werden, damit der Inline-Guard im DELETE-Handler greifen kann.

Ersetze den bestehenden Block (Zeile 53-62):

```javascript
// Admin-only guard for DELETE requests (except calendar appointments)
app.use((req, res, next) => {
  if (req.method === 'DELETE') {
    const permission = req.headers['x-user-permission'];
    if (permission !== 'Admin' && !req.path.startsWith('/api/calendar/') && !req.path.startsWith('/api/time/') && !req.path.startsWith('/api/files/')) {
      return res.status(403).json({ error: 'Nur Admins dürfen Einträge löschen' });
    }
  }
  next();
});
```

durch:

```javascript
// Admin-only guard for DELETE requests (except calendar, time, files, payments)
app.use((req, res, next) => {
  if (req.method === 'DELETE') {
    const permission = req.headers['x-user-permission'];
    if (permission !== 'Admin'
        && !req.path.startsWith('/api/calendar/')
        && !req.path.startsWith('/api/time/')
        && !req.path.startsWith('/api/files/')
        && !req.path.startsWith('/api/payments/')) {
      return res.status(403).json({ error: 'Nur Admins dürfen Einträge löschen' });
    }
  }
  next();
});
```

**Schritt B: GET /api/invoices/:id/payments einfügen.**

Füge in server.js NACH dem bestehenden DELETE /api/invoice-items/:id Handler (Zeile ~1442) und VOR dem `// PDF Generation`-Kommentar (Zeile ~1444) den folgenden Code ein:

```javascript
// ===== INVOICE PAYMENTS (Phase 4 — Zahlungsverwaltung) =====

// PAY-API-01: Liste aller Zahlungen einer Rechnung, sortiert nach payment_date ASC
app.get('/api/invoices/:id/payments', (req, res) => {
  const invoiceId = Number(req.params.id);
  if (!invoiceId) return res.status(400).json({ error: 'Ungültige Rechnungs-ID' });
  const invoice = queryOne('SELECT id FROM invoices WHERE id = ?', [invoiceId]);
  if (!invoice) return res.status(404).json({ error: 'Rechnung nicht gefunden' });
  const payments = queryAll(
    `SELECT p.*, b.label AS bank_account_label, b.iban AS bank_account_iban
     FROM invoice_payments p
     LEFT JOIN bank_accounts b ON p.bank_account_id = b.id
     WHERE p.invoice_id = ?
     ORDER BY p.payment_date ASC, p.id ASC`,
    [invoiceId]
  );
  res.json(payments);
});
```

**Kritische Detail-Regeln:**
- GET hat KEINEN Permission-Guard — Lesezugriff ist für alle Rollen offen
- Sortierung explizit: `ORDER BY p.payment_date ASC, p.id ASC` (sekundär nach id für Stabilität bei gleichem Datum)
- LEFT JOIN auf bank_accounts (NICHT INNER JOIN) — Bar-Buchungen ohne Bankkonto müssen erscheinen
- Rechnungs-Existenz wird vorab geprüft → 404 bei nicht-existierender Rechnung; bei existierender Rechnung ohne Zahlungen wird `[]` (200) zurückgegeben
- Position im File: NACH `app.delete('/api/invoice-items/:id', ...)` (Zeile ~1442), VOR `// PDF Generation` (Zeile ~1444). Alle 4 neuen Endpoints werden in einem zusammenhängenden Block eingefügt — Task 2 ergänzt POST/PUT/DELETE direkt darunter.

**Do NOT:**
- Permission-Guard im GET-Handler einfügen
- ORDER BY DESC verwenden (Anforderung ist explizit ASC)
- Den GET-Handler ohne Existenz-Check der Rechnung lassen (404 bei unbekannter ID liefert klare Semantik fürs Frontend)
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const src=fs.readFileSync('server.js','utf8');const checks=[\"!req.path.startsWith('/api/payments/')\",\"app.get('/api/invoices/:id/payments'\",'INVOICE PAYMENTS','ORDER BY p.payment_date ASC','LEFT JOIN bank_accounts'];const missing=checks.filter(c=>!src.includes(c));if(missing.length)throw new Error('Missing in server.js: '+missing.join(' | '));console.log('All Task 1 patterns present');console.log('PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - server.js enthält den String `!req.path.startsWith('/api/payments/')` im globalen DELETE-Middleware (Zeilen 53-65)
    - server.js enthält den String `app.get('/api/invoices/:id/payments'`
    - server.js enthält den String `ORDER BY p.payment_date ASC, p.id ASC`
    - server.js enthält den String `LEFT JOIN bank_accounts b ON p.bank_account_id = b.id`
    - server.js enthält den String `// ===== INVOICE PAYMENTS (Phase 4 — Zahlungsverwaltung) =====`
    - GET-Handler enthält KEIN `x-user-permission`-Check
    - GET-Handler liefert 404 für nicht-existierende invoice_id (Existenzprüfung via queryOne)
    - Bestehende Endpoints `/api/invoices`, `/api/invoices/:id`, `/api/invoice-items/:id` sind UNVERÄNDERT
    - Server startet ohne Syntaxfehler: `node -c server.js` succeeds (oder `node -e "require('./server.js')"`)
  </acceptance_criteria>
  <done>
    Globales DELETE-Middleware lässt `/api/payments/` durch. GET /api/invoices/:id/payments liefert die Zahlungen einer Rechnung sortiert nach payment_date ASC, joined mit bank_accounts für Label/IBAN. PAY-API-01 ist erfüllt.
  </done>
</task>

<task type="auto">
  <name>Task 2: POST/PUT/DELETE-Endpoints mit Permission-Guards einfügen (PAY-API-02 bis PAY-API-05)</name>
  <files>server.js</files>
  <read_first>
    - C:/Claude/Bemo_v2/server.js (lies den GET /api/invoices/:id/payments Handler aus Task 1 — die neuen 3 Handler werden DIREKT darunter eingefügt, im selben Block "INVOICE PAYMENTS")
    - C:/Claude/Bemo_v2/server.js Zeilen 1330-1385 (POST/PUT/DELETE /api/invoices — exakte Permission-Guard-Logik kopieren)
    - C:/Claude/Bemo_v2/server.js Zeile 3037 (`req.headers['x-user-name']` Pattern für booked_by)
    - C:/Claude/Bemo_v2/.planning/REQUIREMENTS.md (PAY-API-02, PAY-API-03, PAY-API-04, PAY-API-05 — exakte Endpoint-Signaturen)
  </read_first>
  <action>
Füge die folgenden drei Handler in server.js DIREKT NACH dem GET /api/invoices/:id/payments Handler aus Task 1 ein, im selben "INVOICE PAYMENTS"-Block. Reihenfolge: POST → PUT → DELETE.

**POST /api/invoices/:id/payments — neue Zahlung anlegen (PAY-API-02, PAY-API-05):**

```javascript
// PAY-API-02 + PAY-API-05: Neue Zahlung anlegen, booked_by automatisch aus x-user-name
app.post('/api/invoices/:id/payments', (req, res) => {
  // Permission-Guard
  const permission = req.headers['x-user-permission'];
  if (!['Verwaltung', 'Buchhaltung', 'Admin'].includes(permission)) {
    return res.status(403).json({ error: 'Keine Berechtigung' });
  }
  const invoiceId = Number(req.params.id);
  if (!invoiceId) return res.status(400).json({ error: 'Ungültige Rechnungs-ID' });
  const invoice = queryOne('SELECT id FROM invoices WHERE id = ?', [invoiceId]);
  if (!invoice) return res.status(404).json({ error: 'Rechnung nicht gefunden' });

  const { direction, amount, payment_date, payment_method, bank_account_id, reference, notes } = req.body;

  // Body-Validation (vor SQL — klare 400-Antworten)
  if (!direction || !['in', 'out'].includes(direction)) {
    return res.status(400).json({ error: "direction muss 'in' oder 'out' sein" });
  }
  const amt = Number(amount);
  if (!amt || amt <= 0) {
    return res.status(400).json({ error: 'amount muss > 0 sein' });
  }
  if (!payment_date || !/^\d{4}-\d{2}-\d{2}$/.test(payment_date)) {
    return res.status(400).json({ error: 'payment_date muss im Format YYYY-MM-DD sein' });
  }

  // booked_by automatisch aus Header (nicht aus Body)
  const bookedBy = req.headers['x-user-name'] || '';

  try {
    const result = execute(
      `INSERT INTO invoice_payments
        (invoice_id, direction, amount, payment_date, payment_method, bank_account_id, reference, notes, booked_by)
       VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?)`,
      [
        invoiceId,
        direction,
        amt,
        payment_date,
        payment_method || '',
        bank_account_id || null,
        reference || '',
        notes || '',
        bookedBy
      ]
    );
    const created = queryOne('SELECT * FROM invoice_payments WHERE id = ?', [result.lastId]);
    res.status(201).json(created);
  } catch (e) {
    res.status(500).json({ error: 'Zahlung konnte nicht angelegt werden: ' + e.message });
  }
});
```

**PUT /api/payments/:id — Zahlung ändern (PAY-API-03, PAY-API-05):**

```javascript
// PAY-API-03 + PAY-API-05: Zahlung ändern (alle Felder außer booked_by und created_at)
app.put('/api/payments/:id', (req, res) => {
  // Permission-Guard
  const permission = req.headers['x-user-permission'];
  if (!['Verwaltung', 'Buchhaltung', 'Admin'].includes(permission)) {
    return res.status(403).json({ error: 'Keine Berechtigung' });
  }
  const paymentId = Number(req.params.id);
  if (!paymentId) return res.status(400).json({ error: 'Ungültige Zahlungs-ID' });
  const existing = queryOne('SELECT * FROM invoice_payments WHERE id = ?', [paymentId]);
  if (!existing) return res.status(404).json({ error: 'Zahlung nicht gefunden' });

  const { direction, amount, payment_date, payment_method, bank_account_id, reference, notes } = req.body;

  // Body-Validation (gleiche Regeln wie POST)
  if (direction !== undefined && !['in', 'out'].includes(direction)) {
    return res.status(400).json({ error: "direction muss 'in' oder 'out' sein" });
  }
  const amt = (amount !== undefined) ? Number(amount) : existing.amount;
  if (!amt || amt <= 0) {
    return res.status(400).json({ error: 'amount muss > 0 sein' });
  }
  if (payment_date !== undefined && !/^\d{4}-\d{2}-\d{2}$/.test(payment_date)) {
    return res.status(400).json({ error: 'payment_date muss im Format YYYY-MM-DD sein' });
  }

  // PAY-API-03: booked_by und created_at sind UNVERÄNDERLICH — werden hier NICHT in UPDATE-SQL aufgenommen
  try {
    execute(
      `UPDATE invoice_payments SET
         direction = ?,
         amount = ?,
         payment_date = ?,
         payment_method = ?,
         bank_account_id = ?,
         reference = ?,
         notes = ?,
         updated_at = CURRENT_TIMESTAMP
       WHERE id = ?`,
      [
        direction || existing.direction,
        amt,
        payment_date || existing.payment_date,
        (payment_method !== undefined) ? payment_method : existing.payment_method,
        (bank_account_id !== undefined) ? (bank_account_id || null) : existing.bank_account_id,
        (reference !== undefined) ? reference : existing.reference,
        (notes !== undefined) ? notes : existing.notes,
        paymentId
      ]
    );
    const updated = queryOne('SELECT * FROM invoice_payments WHERE id = ?', [paymentId]);
    res.json(updated);
  } catch (e) {
    res.status(500).json({ error: 'Zahlung konnte nicht aktualisiert werden: ' + e.message });
  }
});
```

**DELETE /api/payments/:id — Zahlung löschen (PAY-API-04, PAY-API-05):**

```javascript
// PAY-API-04 + PAY-API-05: Zahlung löschen
app.delete('/api/payments/:id', (req, res) => {
  // Permission-Guard (das globale Middleware lässt /api/payments/ durch — siehe Zeile 53)
  const permission = req.headers['x-user-permission'];
  if (!['Verwaltung', 'Buchhaltung', 'Admin'].includes(permission)) {
    return res.status(403).json({ error: 'Keine Berechtigung' });
  }
  const paymentId = Number(req.params.id);
  if (!paymentId) return res.status(400).json({ error: 'Ungültige Zahlungs-ID' });
  const existing = queryOne('SELECT id FROM invoice_payments WHERE id = ?', [paymentId]);
  if (!existing) return res.status(404).json({ error: 'Zahlung nicht gefunden' });
  execute('DELETE FROM invoice_payments WHERE id = ?', [paymentId]);
  res.json({ success: true, message: 'Zahlung gelöscht' });
});
```

**Kritische Detail-Regeln:**

1. **Permission-Guard exakt:** `if (!['Verwaltung', 'Buchhaltung', 'Admin'].includes(permission))` — Reihenfolge der Rollen muss EXAKT diese sein (analog zu /api/invoices Zeile 1333). Fehlermeldung exakt: `'Keine Berechtigung'`.

2. **Permission-Guard ist FIRST in jedem Handler** — vor jeder anderen Logik (vor req.body-Lesen, vor DB-Zugriff).

3. **booked_by wird ausschließlich aus `req.headers['x-user-name']` gelesen.** NIEMALS aus dem Body — selbst wenn der Body ein `booked_by`-Feld enthält, wird es ignoriert. Pattern aus server.js Zeile 3037: `req.headers['x-user-name'] || ''`.

4. **PUT lässt booked_by und created_at unverändert** — sie kommen nicht im UPDATE-SQL vor. Die SET-Klausel enthält genau 7 Datenspalten + updated_at = 8 Zuweisungen.

5. **Bestehende Werte bei PUT erhalten:** Wenn ein Feld im Body undefined ist, wird der Wert aus `existing` genommen (Pattern: `(payment_date !== undefined) ? payment_date : existing.payment_date`). Das verhindert, dass partielle Updates Felder auf '' überschreiben.

6. **Validation vor SQL:** Body-Validation vor INSERT/UPDATE schickt klare 400-Antworten mit beschreibenden Fehlern (vermeidet 500-Fehler aus CHECK-Constraint-Verletzungen).

7. **HTTP-Status-Codes exakt:**
   - POST success → 201 + JSON-Objekt mit ID
   - PUT success → 200 + JSON-Objekt
   - DELETE success → 200 + `{success: true, message: 'Zahlung gelöscht'}`
   - 403 → fehlende/falsche Berechtigung
   - 400 → invalider Body / invalide ID
   - 404 → invoice_id oder payment_id existiert nicht
   - 500 → SQL-Fehler (z.B. unerwartete CHECK-Verletzung)

8. **bank_account_id Handling:** `bank_account_id || null` wandelt 0/''/undefined in NULL um (Bar-Buchungen ohne Konto-FK erlaubt).

**Do NOT:**
- booked_by aus dem Request-Body lesen (Sicherheit: User darf nicht behaupten, jemand anderes habe gebucht)
- created_at im UPDATE-SQL setzen (PAY-API-03 explizit ausgeschlossen)
- DELETE-Handler weglassen, in der Annahme das globale Middleware reiche (das Middleware blockiert ALLE non-Admin DELETEs OHNE Whitelist — siehe Task 1 Schritt A)
- Soft-Delete implementieren (PAY-EXT-06 ist v2-deferred)
- Bei PUT die booked_by/created_at-Werte aus dem Body verarbeiten (auch wenn das Frontend sie schickt — sie werden ignoriert)
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const src=fs.readFileSync('server.js','utf8');const checks=[\"app.post('/api/invoices/:id/payments'\",\"app.put('/api/payments/:id'\",\"app.delete('/api/payments/:id'\",\"['Verwaltung', 'Buchhaltung', 'Admin'].includes(permission)\",'INSERT INTO invoice_payments','UPDATE invoice_payments SET','DELETE FROM invoice_payments WHERE id','x-user-name','Keine Berechtigung'];const missing=checks.filter(c=>!src.includes(c));if(missing.length)throw new Error('Missing in server.js: '+missing.join(' | '));const postCount=(src.match(/'Keine Berechtigung'/g)||[]).length;if(postCount<3)throw new Error('Erwartet mindestens 3 Permission-Guard-Strings für POST/PUT/DELETE invoice_payments, gefunden: '+postCount+' total');const updateBlock=src.substring(src.indexOf('UPDATE invoice_payments SET'),src.indexOf('UPDATE invoice_payments SET')+800);if(updateBlock.includes('booked_by ='))throw new Error('PUT-UPDATE darf booked_by nicht setzen!');if(updateBlock.includes('created_at ='))throw new Error('PUT-UPDATE darf created_at nicht setzen!');console.log('PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - server.js enthält den String `app.post('/api/invoices/:id/payments'`
    - server.js enthält den String `app.put('/api/payments/:id'`
    - server.js enthält den String `app.delete('/api/payments/:id'`
    - Jeder der drei Handler enthält den String `if (!['Verwaltung', 'Buchhaltung', 'Admin'].includes(permission))`
    - Jeder der drei Handler enthält den String `return res.status(403).json({ error: 'Keine Berechtigung' })`
    - server.js enthält im POST-Handler `req.headers['x-user-name']` für booked_by
    - server.js POST-INSERT enthält 9 Spalten: invoice_id, direction, amount, payment_date, payment_method, bank_account_id, reference, notes, booked_by (9 Question-Marks im VALUES)
    - server.js PUT-UPDATE-SQL enthält genau 8 SET-Zuweisungen (direction, amount, payment_date, payment_method, bank_account_id, reference, notes, updated_at) — und KEINEN `booked_by =`-Zuweisung und KEINEN `created_at =`-Zuweisung
    - server.js DELETE-Handler enthält `DELETE FROM invoice_payments WHERE id = ?`
    - server.js POST validiert direction und amount und payment_date VOR dem SQL-Insert
    - server.js POST liefert HTTP 201 für erfolgreiche Anlage (`res.status(201).json(...)`)
    - server.js DELETE liefert `{success: true, message: 'Zahlung gelöscht'}`
    - Manuell mit Server: `curl -X POST http://localhost:3001/api/invoices/1/payments -H "x-user-permission: Benutzer" -H "Content-Type: application/json" -d '{"direction":"in","amount":100,"payment_date":"2026-04-28"}'` liefert HTTP 403 + `{"error":"Keine Berechtigung"}`
    - Manuell: `curl -X POST http://localhost:3001/api/invoices/{validId}/payments -H "x-user-permission: Verwaltung" -H "x-user-name: testuser" -H "Content-Type: application/json" -d '{"direction":"in","amount":250,"payment_date":"2026-04-28","payment_method":"Überweisung"}'` liefert HTTP 201 + JSON-Objekt mit `id`, `booked_by: "testuser"`, `direction: "in"`, `amount: 250`
    - Manuell: `curl -X POST .../payments -H "x-user-permission: Admin" -d '{"direction":"other","amount":10,"payment_date":"2026-04-28"}'` liefert HTTP 400 + `{"error":"direction muss 'in' oder 'out' sein"}`
    - Manuell: `curl -X DELETE http://localhost:3001/api/payments/{id} -H "x-user-permission: Buchhaltung"` liefert HTTP 200 (NICHT 403 vom globalen Middleware — `/api/payments/` ist whitelisted)
    - Manuell: `curl -X DELETE http://localhost:3001/api/payments/{id} -H "x-user-permission: Benutzer"` liefert HTTP 403 + `{"error":"Keine Berechtigung"}` (Inline-Guard greift)
  </acceptance_criteria>
  <done>
    Alle 4 CRUD-Endpoints existieren und funktionieren: GET (offen), POST (201, setzt booked_by aus Header), PUT (alle Felder außer booked_by und created_at), DELETE (Hard-Delete). Permission-Guard auf POST/PUT/DELETE für ['Verwaltung','Buchhaltung','Admin']; Benutzer-Rolle wird mit 403 abgewiesen. PAY-API-01 bis PAY-API-05 sind erfüllt.
  </done>
</task>

</tasks>

<verification>
End-to-end-Verifikation. Server muss laufen auf Port 3001.

**Server starten:**
```bash
node server.js &
```

**1. PAY-API-01 — GET liefert sortierte Liste (mit Test-Rechnung ID 1):**
```bash
curl -s http://localhost:3001/api/invoices/1/payments | head -c 200
# Erwartung: HTTP 200, JSON-Array (leer oder mit Zahlungen sortiert nach payment_date ASC)
```

**2. PAY-API-05 — POST mit Benutzer-Rolle abgewiesen:**
```bash
curl -s -o /dev/null -w "%{http_code}\n" -X POST http://localhost:3001/api/invoices/1/payments \
  -H "Content-Type: application/json" \
  -H "x-user-permission: Benutzer" \
  -H "x-user-name: bob" \
  -d '{"direction":"in","amount":100,"payment_date":"2026-04-28"}'
# Erwartung: 403
```

**3. PAY-API-02 — POST mit Verwaltung erfolgreich, booked_by aus Header:**
```bash
curl -s -X POST http://localhost:3001/api/invoices/1/payments \
  -H "Content-Type: application/json" \
  -H "x-user-permission: Verwaltung" \
  -H "x-user-name: alice" \
  -d '{"direction":"in","amount":250.50,"payment_date":"2026-04-28","payment_method":"Überweisung","reference":"Re-Nr 042026001"}'
# Erwartung: HTTP 201, JSON mit "id", "booked_by":"alice", "direction":"in", "amount":250.5
```

**4. PAY-API-02 — booked_by-Body-Wert wird IGNORIERT:**
```bash
curl -s -X POST http://localhost:3001/api/invoices/1/payments \
  -H "Content-Type: application/json" \
  -H "x-user-permission: Admin" \
  -H "x-user-name: admin1" \
  -d '{"direction":"in","amount":50,"payment_date":"2026-04-28","booked_by":"hacker"}'
# Erwartung: booked_by im Response = "admin1" (NICHT "hacker")
```

**5. CHECK-Constraint via Body-Validation aufgefangen — direction='other':**
```bash
curl -s -o /dev/null -w "%{http_code}\n" -X POST http://localhost:3001/api/invoices/1/payments \
  -H "Content-Type: application/json" \
  -H "x-user-permission: Admin" \
  -H "x-user-name: admin1" \
  -d '{"direction":"other","amount":10,"payment_date":"2026-04-28"}'
# Erwartung: 400
```

**6. PAY-API-03 — PUT ändert Felder, lässt booked_by unverändert:**
```bash
# Annahme: ID des in Schritt 3 angelegten Datensatzes ist {ID}
curl -s -X PUT http://localhost:3001/api/payments/{ID} \
  -H "Content-Type: application/json" \
  -H "x-user-permission: Buchhaltung" \
  -H "x-user-name: zwischenuser" \
  -d '{"amount":300,"payment_method":"Lastschrift","booked_by":"hacker"}'
# Erwartung: HTTP 200, amount=300, payment_method="Lastschrift", booked_by="alice" (unverändert)
```

**7. PAY-API-04 — DELETE durch Buchhaltung erfolgreich (globales Middleware lässt durch, Inline-Guard erlaubt 3 Rollen):**
```bash
curl -s -o /dev/null -w "%{http_code}\n" -X DELETE http://localhost:3001/api/payments/{ID} \
  -H "x-user-permission: Buchhaltung"
# Erwartung: 200
```

**8. PAY-API-05 — DELETE durch Benutzer abgewiesen:**
```bash
curl -s -o /dev/null -w "%{http_code}\n" -X DELETE http://localhost:3001/api/payments/9999 \
  -H "x-user-permission: Benutzer"
# Erwartung: 403
```

**9. PAY-DB-02 (Smoke-Test): DELETE invoice cascade:**
```bash
# Wenn eine Test-Rechnung mit Zahlungen existiert: nach DELETE invoice darf GET payments leer sein
# (siehe ROADMAP Phase 4 Success Criterion 2)
```
</verification>

<success_criteria>
1. GET /api/invoices/:id/payments liefert chronologisch sortiertes Array (PAY-API-01)
2. POST /api/invoices/:id/payments legt eine Zahlung an, gibt HTTP 201 zurück, setzt booked_by aus x-user-name Header (PAY-API-02)
3. PUT /api/payments/:id ändert alle Felder außer booked_by und created_at (PAY-API-03)
4. DELETE /api/payments/:id löscht eine Buchung (PAY-API-04)
5. POST/PUT/DELETE mit x-user-permission: Benutzer → HTTP 403 + {error: 'Keine Berechtigung'} (PAY-API-05)
6. POST/PUT/DELETE mit x-user-permission: Verwaltung/Buchhaltung/Admin → erfolgreich (PAY-API-05)
7. Body-Validation fängt direction != 'in'/'out', amount <= 0, ungültiges payment_date mit HTTP 400 ab
8. booked_by aus dem Request-Body wird ignoriert — nur x-user-name Header wird verwendet
9. GET-Endpoint hat KEINEN Permission-Guard (Lesezugriff offen)
</success_criteria>

<output>
After completion, create `.planning/phases/04-schema-backend/04-02-SUMMARY.md`
</output>
